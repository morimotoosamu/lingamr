% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var.r
\name{lingam_var}
\alias{lingam_var}
\title{VAR-LiNGAM for time series causal discovery}
\usage{
lingam_var(
  X,
  lags = 1L,
  criterion = "bic",
  measure = "pwling",
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  prune = TRUE
)
}
\arguments{
\item{X}{numeric matrix or data frame (n_samples x n_features). Rows are
ordered in time (earliest first).}

\item{lags}{maximum lag order. When \code{criterion} is not NULL, the best lag
in \code{1:lags} is selected by the information criterion; otherwise \code{lags} is
used directly.}

\item{criterion}{lag-selection criterion ("bic", "aic", "hqic", or "fpe"),
or NULL to use \code{lags} directly without selection.}

\item{measure}{independence measure passed to \code{\link[=lingam_direct]{lingam_direct()}}
("pwling" or "kernel").}

\item{reg_method}{regression method for the instantaneous adjacency matrix:
"adaptive_lasso" (default), "lasso", "ols", or "ridge" (see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{lambda}{penalty (lambda) selection for the instantaneous matrix:
"BIC" (default), "AIC", "lambda.min", "lambda.1se", or "oracle"
(see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{init_method}{initial-weight method for adaptive LASSO
(see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{prune}{logical; if \code{TRUE} (default, matching the Python reference),
all adjacency matrices (instantaneous B0 and the lagged B_k) are refined
together by adaptive LASSO so weak edges are shrunk toward zero. Requires
the glmnet package. Set \code{FALSE} to keep the raw \verb{B_k = (I - B0) M_k}
matrices (no glmnet needed when \code{reg_method = "ols"}).}
}
\value{
A \code{VARLiNGAMResult} object (list) containing:
\itemize{
\item \code{adjacency_matrices}: array (1 + lags, n_features, n_features). The first
slice \verb{[1, , ]} is the instantaneous matrix B0; slice \verb{[k + 1, , ]} is the
lagged matrix B_k for lag k (k = 1..lags). Convention: \code{B[i, j]} is the
effect from variable j to variable i.
\item \code{causal_order}: estimated causal order of the instantaneous structure
(1-based indices).
\item \code{residuals}: VAR residuals (n_samples - lags, n_features).
\item \code{lags}: the lag order actually used.
}
}
\description{
Fits a vector autoregressive (VAR) model to time series data and applies
Direct LiNGAM to the residuals to recover the instantaneous (lag-0) causal
structure. The lagged causal matrices are then derived from the VAR
coefficients and the instantaneous structure.
}
\details{
The model is \verb{X_t = B0 X_t + sum_\{k=1\}^\{p\} B_k X_\{t-k\} + e_t}, where B0 is
the instantaneous effect matrix (strictly acyclic) and e_t are mutually
independent non-Gaussian errors. VAR coefficients \code{M_k} are estimated by
ordinary least squares (no intercept); residuals \verb{e_t = X_t - sum M_k X_\{t-k\}}
are passed to \code{\link[=lingam_direct]{lingam_direct()}} to obtain B0, and the lagged matrices follow
\verb{B_k = (I - B0) M_k}.
}
\examples{
sample <- generate_varlingam_sample(n = 500, seed = 42)

# OLS instantaneous structure without pruning (no extra packages required)
model <- lingam_var(sample$data, lags = 1, reg_method = "ols", prune = FALSE)
round(model$adjacency_matrices[1, , ], 2)  # instantaneous B0
}
\references{
Hyvärinen, A., Zhang, K., Shimizu, S., & Hoyer, P. O. (2010). Estimation of a
structural vector autoregression model using non-Gaussianity. \emph{Journal of
Machine Learning Research}, 11, 1709-1731. Ported from the Python
implementation cdt15/lingam (\url{https://github.com/cdt15/lingam}). See also the
VARLiNGAM R code of Moneta et al.
(\url{https://sites.google.com/site/dorisentner/publications/VARLiNGAM}).
}
