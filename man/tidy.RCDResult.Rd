% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.RCDResult}
\alias{tidy.RCDResult}
\title{Convert an RCDResult to a tidy data.frame}
\usage{
\method{tidy}{RCDResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_rcd]{lingam_rcd()}} (an \code{RCDResult} object)}

\item{threshold}{Coefficients with an absolute value at or below this are not
treated as edges (default: 0). \code{NA} entries are always kept.}

\item{...}{Unused}
}
\value{
data.frame(from, to, estimate)
}
\description{
Converts the estimated adjacency matrix into a long-format data.frame with
one edge per row, like \code{\link[=tidy.LingamResult]{tidy.LingamResult()}}. \code{NA} entries of the adjacency
matrix (variable pairs suspected to share a latent confounder; marked in
both directions) are kept as rows with \code{estimate = NA} so they remain
visible; drop them with e.g. \code{subset(tidy(x), !is.na(estimate))} if not
needed.
}
\examples{
confounded <- generate_rcd_sample(n = 300, seed = 1)
model <- lingam_rcd(confounded$data)
tidy(model)
}
