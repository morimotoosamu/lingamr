% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma.r
\name{prune_varma_lingam}
\alias{prune_varma_lingam}
\title{Prune VARMA-LiNGAM adjacency matrices by adaptive LASSO}
\usage{
prune_varma_lingam(
  X,
  ee_full,
  causal_order,
  order,
  lambda = "BIC",
  init_method = "ols"
)
}
\arguments{
\item{X}{numeric matrix (n_samples x n_features), rows ordered in time}

\item{ee_full}{LiNGAM residuals \verb{e_t = (I - B0) n_t}, full length
(n_samples rows; the first max(p, q) rows are zero)}

\item{causal_order}{instantaneous causal order (1-based indices)}

\item{order}{VARMA order c(p, q)}

\item{lambda}{lambda selection passed to \code{\link[=fit_adaptive_lasso]{fit_adaptive_lasso()}}}

\item{init_method}{initial-weight method for adaptive LASSO}
}
\value{
list with \code{psis} (array (1 + p, m, m); slice 1 is B0) and \code{omegas}
(array (q, m, m))
}
\description{
Re-estimates the instantaneous matrix B0, every lagged matrix psi_tau, and
every MA matrix omega_w jointly, shrinking weak edges to zero. Port of the
Python reference \verb{_pruning}. For each target variable, the predictors are
its contemporaneous ancestors (those preceding it in \code{causal_order}) plus
all variables at lags 1..p and the LiNGAM residuals e_t at lags 1..q.
Unlike the Python reference (which wraps rows around with \code{np.roll}), the
regression window is restricted to rows where every regressor is observed.
}
\keyword{internal}
