% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_multi_group_sample.r
\name{generate_multi_group_sample}
\alias{generate_multi_group_sample}
\title{Generate sample data for Multi-Group Direct LiNGAM (2 groups, 6 variables)}
\usage{
generate_multi_group_sample(n = c(1000, 1000), seed = 42L)
}
\arguments{
\item{n}{Numeric vector of length 2: sample size per group (default \code{c(1000, 1000)}).}

\item{seed}{Random seed (default 42). Group 2 uses an internally offset
seed so the two groups are independently drawn.}
}
\value{
A list with:
\itemize{
\item \code{data_list}: named list (\code{group1}, \code{group2}) of data frames, each with
columns \code{x0}..\code{x5}.
\item \code{adjacency_matrices}: named list of the true adjacency matrix per group
(\code{m[to, from] = coef}, same convention as \code{\link[=lingam_direct]{lingam_direct()}}).
\item \code{causal_order}: the true causal order shared by both groups (1-based
indices into \code{x0}..\code{x5}).
}
}
\description{
Generates two datasets that share the same causal structure as
\code{\link[=generate_lingam_sample_6]{generate_lingam_sample_6()}} (\verb{x3 -> x0, x3 -> x2, x0 -> x1, x2 -> x1, x0 -> x4, x2 -> x4, x0 -> x5}) but with different structural coefficients
per group, following the multi-dataset tutorial's setup.
}
\examples{
mg <- generate_multi_group_sample()
lapply(mg$data_list, head)
mg$adjacency_matrices$group1
}
