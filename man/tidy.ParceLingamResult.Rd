% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.ParceLingamResult}
\alias{tidy.ParceLingamResult}
\title{Convert a ParceLingamResult to a tidy data.frame}
\usage{
\method{tidy}{ParceLingamResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_parce]{lingam_parce()}} (a \code{ParceLingamResult} object)}

\item{threshold}{Coefficients with an absolute value at or below this are not
treated as edges (default: 0). \code{NA} entries are always kept.}

\item{...}{Unused}
}
\value{
data.frame(from, to, estimate)
}
\description{
Converts the estimated adjacency matrix into a long-format data.frame with
one edge per row, like \code{\link[=tidy.LingamResult]{tidy.LingamResult()}}. \code{NA} entries of the adjacency
matrix (variable pairs whose order could not be resolved / suspected latent
confounding) are kept as rows with \code{estimate = NA} so they remain visible;
drop them with e.g. \code{subset(tidy(x), !is.na(estimate))} if not needed.
}
\examples{
dat <- generate_parce_sample(n = 500, seed = 42)
model <- lingam_parce(dat$data)
tidy(model)
}
