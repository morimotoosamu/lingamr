% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_model_fit.r
\name{evaluate_model_fit}
\alias{evaluate_model_fit}
\title{Evaluate model fit of an estimated causal graph}
\usage{
evaluate_model_fit(adjacency_matrix, X, is_ordinal = NULL)
}
\arguments{
\item{adjacency_matrix}{p x p numeric adjacency matrix (NA allowed for
latent confounder pairs), or a lingamr result object (e.g.
\code{LingamResult}, \code{ParceLingamResult}, \code{LiMResult}) with an
\code{adjacency_matrix} element, from which the matrix is extracted
automatically}

\item{X}{numeric matrix or data frame (n_samples x p) with no missing
values}

\item{is_ordinal}{logical or 0/1 vector of length p. \code{TRUE} marks a
variable as ordinal (categorical), fit with \code{lavaan}'s WLSMV-based
estimator. \code{NULL} (default) treats all variables as continuous.}
}
\value{
A one-row data.frame of fit measures: DoF, DoF Baseline, chi2,
chi2 p-value, chi2 Baseline, CFI, GFI, AGFI, NFI, TLI, RMSEA, AIC,
BIC, LogLik. When \code{is_ordinal} is used, AIC/BIC/LogLik and some other
measures are not defined by the WLSMV estimator and are returned as
\code{NA}.
}
\description{
Fits the causal graph implied by \code{adjacency_matrix} as a structural
equation model (SEM) via \code{lavaan::sem()} and returns standard SEM fit
measures (CFI, RMSEA, AIC/BIC, etc.). This is an R port of the Python
\code{lingam.utils.evaluate_model_fit()}, which delegates to the Python
package \code{semopy}; this R version delegates to \code{lavaan} instead.
}
\details{
\itemize{
\item \strong{Optional dependency}: this function requires the \pkg{lavaan}
package (listed in \code{Suggests}, not \code{Imports}). Install it with
\code{install.packages("lavaan")}.
\item \strong{Latent confounders}: an \code{NA} element \code{B[i, j]} (as produced by
e.g. \code{\link[=lingam_parce]{lingam_parce()}} for a suspected latent confounder between
variables i and j) is represented as a residual covariance
\code{xi ~~ xj} in the lavaan model. This is algebraically equivalent to
the two-indicator latent common cause (one loading fixed to 1) used
by the Python \code{semopy} implementation, but is expressed with lavaan's
standard residual-covariance idiom rather than an explicit latent
variable.
\item \strong{Numerical values will not match \code{semopy} exactly}: \code{lavaan} and
\code{semopy} use different default estimators/options. The fit measures
returned are the same statistics, but exact numbers can differ.
\item \strong{Convention}: \code{adjacency_matrix} follows the lingamr convention
\code{B[i, j]} = causal coefficient from variable j to variable i (j -> i).
}
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  dat <- generate_lingam_sample_6()
  result <- lingam_direct(dat$data, reg_method = "ols")
  evaluate_model_fit(result, dat$data)
}
}
\references{
Rosseel, Y. (2012). lavaan: An R Package for Structural Equation
Modeling. Journal of Statistical Software, 48(2), 1-36.
\doi{10.18637/jss.v048.i02}
}
