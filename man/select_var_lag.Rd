% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var.r
\name{select_var_lag}
\alias{select_var_lag}
\title{Select the VAR lag order by information criterion}
\usage{
select_var_lag(X, max_lag, criterion = "bic")
}
\arguments{
\item{X}{numeric matrix (n_samples x n_features)}

\item{max_lag}{maximum lag order to consider}

\item{criterion}{"bic", "aic", "hqic", or "fpe"}
}
\value{
the selected lag order (integer)
}
\description{
All candidate lag orders are compared on a \strong{common sample}: the first
\code{max_lag} observations are dropped for every candidate so that each VAR(lag)
is estimated over the same time window (t = max_lag + 1 .. n). This mirrors
statsmodels' \code{VAR.select_order} and makes the criteria comparable across
lags (otherwise a longer lag would be scored on fewer observations).
}
\keyword{internal}
