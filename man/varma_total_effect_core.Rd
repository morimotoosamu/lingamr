% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma_total_effect.r
\name{varma_total_effect_core}
\alias{varma_total_effect_core}
\title{Total-effect regression core for VARMA-LiNGAM}
\usage{
varma_total_effect_core(
  X,
  ee_full,
  am_joined,
  order,
  from_index,
  to_index,
  from_lag,
  X_joined = NULL
)
}
\arguments{
\item{X}{numeric matrix (n x m), rows ordered in time}

\item{ee_full}{LiNGAM residuals \verb{e_t = (I - B0) n_t}, full length (n x m)}

\item{am_joined}{joined causal matrix \code{cbind(psi_0..psi_p, omega_1..omega_q)}
(m x m(1 + p + q))}

\item{order}{VARMA order c(p, q)}

\item{from_index}{source variable (1-based integer)}

\item{to_index}{destination variable (1-based integer)}

\item{from_lag}{lag of the source variable (non-negative integer)}

\item{X_joined}{precomputed \code{\link[=varma_joined_design]{varma_joined_design()}} result, or NULL to
build it here. The design depends only on (X, ee_full, order, from_lag),
so the bootstrap builds it once per lag instead of once per variable pair.}
}
\value{
the estimated total effect (scalar)
}
\description{
Regresses the destination variable on the source variable plus the source's
parents (a back-door adjustment) over a joined design of lagged X and
lagged LiNGAM residuals, and returns the source's coefficient. Shared by
\code{\link[=estimate_varma_total_effect]{estimate_varma_total_effect()}} and the VARMA-LiNGAM bootstrap.
}
\details{
The X region of the design holds lags \verb{0..(p + from_lag)} and the residual
region holds lags \verb{1..(q + from_lag)}, so parents shifted into the
\code{from_lag} block always reference filled columns. (The Python reference
allocates the extra \code{from_lag} blocks but never fills them, so its shifted
predictors can point at zero or misaligned columns when \code{from_lag > 0}.)
}
\keyword{internal}
