% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_multi_group.r
\name{lingam_multi_group}
\alias{lingam_multi_group}
\title{Multi-Group Direct LiNGAM}
\usage{
lingam_multi_group(
  X_list,
  prior_knowledge = NULL,
  apply_prior_knowledge_softly = FALSE,
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols"
)
}
\arguments{
\item{X_list}{A list of numeric matrices or data frames (length >= 2), one
per group. Each element must have \code{n_d} rows (sample size may differ by
group) and the same number of columns \code{p} across all groups.}

\item{prior_knowledge}{Prior knowledge matrix (n_features x n_features) or
NULL. Applied identically to every group. Same encoding as
\code{\link[=lingam_direct]{lingam_direct()}}:
0: no directed path from x_i to x_j
1: directed path from x_i to x_j
-1: unknown}

\item{apply_prior_knowledge_softly}{Whether to apply prior knowledge softly (logical)}

\item{reg_method}{Regression method for adjacency matrix estimation.
"ols": ordinary least squares,
"lasso": LASSO regression,
"adaptive_lasso": adaptive LASSO regression (default),
"ridge": Ridge regression (robust to multicollinearity; does not perform sparse estimation).}

\item{lambda}{LASSO penalty (lambda) selection. Same choices as \code{\link[=lingam_direct]{lingam_direct()}}.}

\item{init_method}{Method for estimating the initial weights of adaptive
LASSO regression ("ols" (default) or "ridge").}
}
\value{
A \code{MultiGroupLingamResult} object (list) containing:
\itemize{
\item \code{adjacency_matrices}: a named list of adjacency matrices, one per group
(name = group name). Each follows the same convention as
\code{\link[=lingam_direct]{lingam_direct()}}: \code{B[i, j]} is the causal coefficient from variable j to
variable i (j -> i).
\item \code{causal_order}: the causal order shared by all groups (integer vector of
1-based indices).
}
}
\description{
Jointly estimates a Direct LiNGAM model from multiple datasets ("groups")
that are assumed to share a common causal order but may have different
structural coefficients (Shimizu 2012).
}
\details{
Unlike \code{\link[=lingam_direct]{lingam_direct()}}, this function has no \code{measure} argument: the
multi-group causal-order search only supports the pwling (pairwise
likelihood / entropy approximation) objective, matching the upstream
Python \code{MultiGroupDirectLiNGAM}, which does not offer a kernel-based
multi-group search.

For downstream analysis of a single group (total causal effects,
independence tests of residuals, plotting), extract that group as a plain
\code{LingamResult} with \code{\link[=get_group_result]{get_group_result()}} and pass it to the existing
single-group functions (\code{\link[=estimate_total_effect]{estimate_total_effect()}},
\code{\link[=estimate_all_total_effects]{estimate_all_total_effects()}}, \code{\link[=get_error_independence_p_values]{get_error_independence_p_values()}},
\code{\link[=plot_adjacency]{plot_adjacency()}}, \code{autoplot()}, \code{tidy()}); no multi-group-specific
wrappers are provided for these.
}
\examples{
mg <- generate_multi_group_sample()
res <- lingam_multi_group(mg$data_list, reg_method = "ols")
print(res)

# Analyze group 1 with the existing single-group tooling
g1 <- get_group_result(res, 1)
estimate_all_total_effects(mg$data_list[[1]], g1, method = "ols")
}
\references{
S. Shimizu. Joint estimation of linear non-Gaussian acyclic models.
Neurocomputing, 81: 104-107, 2012.
}
