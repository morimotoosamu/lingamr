% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_parce.r
\name{estimate_total_effect_parce}
\alias{estimate_total_effect_parce}
\title{Estimate the total causal effect between two variables (ParceLiNGAM)}
\usage{
estimate_total_effect_parce(
  X,
  parce_result,
  from_index,
  to_index,
  method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols"
)
}
\arguments{
\item{X}{Original data (matrix or data.frame)}

\item{parce_result}{Return value of \code{\link[=lingam_parce]{lingam_parce()}}}

\item{from_index}{Cause variable (1-based index or variable name)}

\item{to_index}{Effect variable (1-based index or variable name)}

\item{method}{Regression method ("ols", "lasso", "adaptive_lasso", "ridge"). Default is adaptive_lasso}

\item{lambda}{Lambda selection ("lambda.min", "lambda.1se", "AIC", "BIC", "oracle"). Default is BIC}

\item{init_method}{Method for estimating the initial weights of adaptive LASSO regression ("ols" or "ridge")}
}
\value{
Estimated total causal effect, or \code{NA} (with a warning) if \code{from_index}
is part of an unresolved block (its parents cannot be identified).
}
\description{
Analogous to \code{\link[=estimate_total_effect]{estimate_total_effect()}}, but for \code{\link[=lingam_parce]{lingam_parce()}} results,
which may contain \code{NA} entries in the adjacency matrix.
}
\examples{
confounded <- generate_parce_sample(n = 500, seed = 1)
result <- lingam_parce(confounded$data, reg_method = "ols")

# A well-identified pair returns a numeric estimate
estimate_total_effect_parce(confounded$data, result, from_index = 1, to_index = 5)
}
