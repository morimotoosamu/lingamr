% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.CAMUVResult}
\alias{tidy.CAMUVResult}
\title{Convert a CAMUVResult to a tidy data.frame}
\usage{
\method{tidy}{CAMUVResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_camuv]{lingam_camuv()}} (a \code{CAMUVResult} object)}

\item{threshold}{Kept for interface consistency with the other \code{tidy()}
methods (default: 0); with 0/1 entries any value in \verb{[0, 1)} returns
all edges. \code{NA} entries are always kept.}

\item{...}{Unused}
}
\value{
data.frame(from, to, estimate)
}
\description{
Converts the estimated adjacency matrix into a long-format data.frame with
one edge per row, like \code{\link[=tidy.LingamResult]{tidy.LingamResult()}}. Because CAM-UV is a nonlinear
method, \code{estimate} is 1 for identified edges (an edge indicator, not a
coefficient), and \code{NA} for variable pairs suspected to be connected through
an unobserved variable (UCP/UBP); the \code{NA} rows appear in both directions.
Drop them with e.g. \code{subset(tidy(x), !is.na(estimate))} if not needed.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  confounded <- generate_camuv_sample(n = 200, seed = 1)
  model <- lingam_camuv(confounded$data)
  tidy(model)
}
}
}
