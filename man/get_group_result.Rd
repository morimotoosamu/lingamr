% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_multi_group.r
\name{get_group_result}
\alias{get_group_result}
\title{Extract a single group's result from a MultiGroupLingamResult}
\usage{
get_group_result(x, group)
}
\arguments{
\item{x}{A \code{MultiGroupLingamResult}, as returned by \code{\link[=lingam_multi_group]{lingam_multi_group()}}.}

\item{group}{Group name (character) or 1-based group index (integer).}
}
\value{
A \code{LingamResult} object (list) with \code{adjacency_matrix} and
\code{causal_order}, identical in shape to the return value of
\code{\link[=lingam_direct]{lingam_direct()}}.
}
\description{
Returns the adjacency matrix and (shared) causal order of one group as a
plain \code{LingamResult}, so that the existing single-group functions
(\code{\link[=estimate_total_effect]{estimate_total_effect()}}, \code{\link[=estimate_all_total_effects]{estimate_all_total_effects()}},
\code{\link[=get_error_independence_p_values]{get_error_independence_p_values()}}, \code{\link[=plot_adjacency]{plot_adjacency()}}, \code{autoplot()},
\code{tidy()}) can be applied to it directly.
}
\examples{
mg <- generate_multi_group_sample()
res <- lingam_multi_group(mg$data_list, reg_method = "ols")
g1 <- get_group_result(res, 1)
class(g1)
}
