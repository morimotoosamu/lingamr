% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma_bootstrap.r
\name{get_varma_paths}
\alias{get_varma_paths}
\title{Enumerate bootstrap paths between two variables in a VARMA-LiNGAM model}
\usage{
get_varma_paths(
  result,
  from_index,
  to_index,
  from_lag = 0,
  to_lag = 0,
  min_causal_effect = NULL
)
}
\arguments{
\item{result}{a VARMABootstrapResult object}

\item{from_index}{source variable (1-based)}

\item{to_index}{destination variable (1-based)}

\item{from_lag}{lag of the source (default 0); must not exceed the AR order p}

\item{to_lag}{lag of the destination (default 0); must satisfy \code{to_lag <= from_lag}}

\item{min_causal_effect}{minimum |effect| threshold (NULL = 0)}
}
\value{
a data frame (path, effect, probability), one row per distinct path
}
\description{
Builds the time-expanded graph for every bootstrap sample and enumerates all
directed paths from the source (at \code{from_lag}) to the destination (at
\code{to_lag}), reporting each path's bootstrap probability and median effect.
Port of the Python reference \code{VARMABootstrapResult.get_paths}.
}
\details{
Node indices in the returned \code{path} are 1-based positions in the time-expanded
graph: column j of block L (lag L) corresponds to index \code{n_features * L + j}.

Only the instantaneous and AR (psi) blocks enter the time-expanded graph;
the MA (omega) blocks describe effects of past unobserved disturbances,
which are not nodes of the variable graph (the Python reference does the
same).
}
\examples{
s <- generate_varmalingam_sample(n = 300, seed = 42)
bs <- lingam_varma_bootstrap(s$data,
  n_sampling = 5L, order = c(1, 1), criterion = NULL,
  reg_method = "ols", prune = FALSE, seed = 1, verbose = FALSE
)
get_varma_paths(bs, from_index = 1, to_index = 3)
}
