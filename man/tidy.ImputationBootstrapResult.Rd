% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.ImputationBootstrapResult}
\alias{tidy.ImputationBootstrapResult}
\title{Convert an ImputationBootstrapResult to a tidy data.frame}
\usage{
\method{tidy}{ImputationBootstrapResult}(x, aggregate = c("median", "mean"), ...)
}
\arguments{
\item{x}{The return value of \code{\link[=bootstrap_with_imputation]{bootstrap_with_imputation()}}
(an \code{ImputationBootstrapResult} object)}

\item{aggregate}{How to collapse the \code{n_repeats} imputation dimension,
passed to \code{\link[=as_bootstrap_result]{as_bootstrap_result()}} ("median" or "mean")}

\item{...}{Arguments passed to \code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}}}
}
\value{
data.frame (from, to, count, proportion, ...)
}
\description{
Collapses the imputation dimension with \code{\link[=as_bootstrap_result]{as_bootstrap_result()}} and then
summarizes the causal direction counts like \code{\link[=tidy.BootstrapResult]{tidy.BootstrapResult()}}.
}
\examples{
\dontshow{if (requireNamespace("mice", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dat <- generate_lingam_sample_6(n = 200, seed = 1)$data
dat[sample(nrow(dat), 20), 1] <- NA
bs <- bootstrap_with_imputation(dat, n_sampling = 5L, n_repeats = 2L, seed = 42)
tidy(bs)
\dontshow{\}) # examplesIf}
}
