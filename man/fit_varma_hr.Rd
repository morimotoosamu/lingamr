% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma.r
\name{fit_varma_hr}
\alias{fit_varma_hr}
\title{Fit a VARMA(p, q) model by two-stage Hannan-Rissanen}
\usage{
fit_varma_hr(X, p_order, q_order, h = NULL)
}
\arguments{
\item{X}{numeric matrix (n_samples x n_features), rows ordered in time}

\item{p_order}{AR order (non-negative integer)}

\item{q_order}{MA order (non-negative integer)}

\item{h}{long AR order for stage 1 (NULL = use \code{\link[=hr_long_ar_order]{hr_long_ar_order()}};
0 when q = 0, where stage 1 is skipped)}
}
\value{
list with \code{phis} (array (p, m, m)), \code{thetas} (array (q, m, m)),
\code{const} (numeric m), \code{residuals} (stage-2 OLS residuals, one row per
observation in the estimation window t = h + q + 1 .. n), and \code{h}
}
\description{
Stage 1 fits a long VAR(h) with intercept whose residuals estimate the
innovations; stage 2 regresses X on its own lags 1..p and the stage-1
residual lags 1..q (with intercept) by OLS.
}
\keyword{internal}
