% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{glance.RCDResult}
\alias{glance.RCDResult}
\title{Get a one-row summary of an RCDResult}
\usage{
\method{glance}{RCDResult}(x, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_rcd]{lingam_rcd()}} (an \code{RCDResult} object)}

\item{...}{Unused}
}
\value{
A one-row data.frame(n_variables, n_edges, n_confounded_pairs)
}
\description{
Like \code{\link[=glance.LingamResult]{glance.LingamResult()}}, but without a causal order (RCD does not
estimate one). \code{n_edges} counts non-\code{NA} edges only, and
\code{n_confounded_pairs} counts the variable pairs whose adjacency-matrix
entries are \code{NA} (suspected shared latent confounder).
}
\examples{
confounded <- generate_rcd_sample(n = 300, seed = 1)
model <- lingam_rcd(confounded$data)
glance(model)
}
