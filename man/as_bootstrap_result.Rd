% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_with_imputation.r
\name{as_bootstrap_result}
\alias{as_bootstrap_result}
\title{Collapse an ImputationBootstrapResult into a BootstrapResult}
\usage{
as_bootstrap_result(x, aggregate = c("median", "mean"))
}
\arguments{
\item{x}{An \code{ImputationBootstrapResult}, as returned by
\code{\link[=bootstrap_with_imputation]{bootstrap_with_imputation()}}.}

\item{aggregate}{How to aggregate across the \code{n_repeats} dimension:
\code{"median"} (default) or \code{"mean"}.}
}
\value{
A \code{BootstrapResult} (see \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}) with
\code{total_effects = NULL} (total effects are not computed by
\code{bootstrap_with_imputation()}); calling \code{\link[=get_total_causal_effects]{get_total_causal_effects()}} on
it raises the usual "not computed" error.
}
\description{
\code{bootstrap_with_imputation()}'s result carries an extra \code{n_repeats}
dimension (one causal-structure estimate per imputed dataset per bootstrap
iteration), which the existing bootstrap analysis functions
(\code{\link[=get_probabilities]{get_probabilities()}}, \code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}},
\code{\link[=get_directed_acyclic_graph_counts]{get_directed_acyclic_graph_counts()}}, \code{\link[=get_causal_order_stability]{get_causal_order_stability()}}, \code{tidy()})
do not expect. This collapses that dimension by aggregating the
\code{n_repeats} adjacency matrices of each iteration into one, producing a
regular \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}-style \code{BootstrapResult}.
}
\examples{
set.seed(1)
sample6 <- generate_lingam_sample_6(n = 300, seed = 1)
X <- sample6$data
X$x5[sample.int(nrow(X), size = 30)] <- NA

if (requireNamespace("mice", quietly = TRUE)) {
  res <- bootstrap_with_imputation(X,
    n_sampling = 5L, n_repeats = 3L, seed = 42, verbose = FALSE
  )
  bs <- as_bootstrap_result(res, aggregate = "median")
  get_probabilities(bs)

  # get_total_causal_effects() is not available: total effects were never computed
  tryCatch(get_total_causal_effects(bs), error = function(e) conditionMessage(e))
}
}
