% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_camuv.r
\name{lingam_camuv}
\alias{lingam_camuv}
\title{CAM-UV (Causal Additive Models with Unobserved Variables)}
\usage{
lingam_camuv(
  X,
  alpha = 0.01,
  num_explanatory_vals = 2L,
  independence = "hsic",
  ind_corr = 0.5,
  prior_knowledge = NULL,
  regressor = "gam"
)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features), data frame or matrix}

\item{alpha}{Significance level of the HSIC independence test (used when
\code{independence = "hsic"}). Must be non-negative.}

\item{num_explanatory_vals}{Maximum size of the variable subsets scanned
when searching for parents (the maximum number of explanatory
variables considered jointly is \code{num_explanatory_vals - 1}). Larger
values increase statistical power but grow combinatorially in cost.
Must be an integer of 1 or more.}

\item{independence}{Independence measure: "hsic" (default) uses the HSIC
gamma-approximation test; "fcorr" uses the F-correlation (kernel
canonical correlation) and rejects based on \code{ind_corr} instead of a
p-value.}

\item{ind_corr}{Threshold on the F-correlation value, used only when
\code{independence = "fcorr"} (independence is declared below this value).
Must be non-negative. Ignored when \code{independence = "hsic"}.}

\item{prior_knowledge}{Optional prior knowledge as variable pairs: a
2-column matrix or a list of length-2 vectors, where each pair
\code{c(i, j)} means "variable i cannot be a cause of variable j".
Indices are \strong{1-based} column positions (the Python implementation
uses 0-based pairs).}

\item{regressor}{Nonlinear regressor used for all internal regressions,
same interface as \code{\link[=lingam_resit]{lingam_resit()}}: either the string \code{"gam"}
(default; requires the suggested package mgcv) or a function
\verb{function(X, y)} returning the fitted values as a numeric vector of
length \code{nrow(X)}. The Python implementation hardcodes pygam's
\code{LinearGAM}, whose spline basis differs from mgcv's, so numerical
agreement with Python is not expected (structural results are).}
}
\value{
An object of class \code{CAMUVResult} with elements:
\itemize{
\item \code{adjacency_matrix}: (p x p) matrix. \strong{Convention: \code{B[i, j] = 1} means
an edge j -> i (row = to, col = from)}, same as \code{\link[=lingam_direct]{lingam_direct()}}.
Entries are 0/1 edge indicators, not coefficients (the causal
functions are nonlinear). Both entries of a variable pair suspected to
be connected by a UCP or UBP are \code{NA}.
\item \code{parents_list}: a list of length \code{n_features}; element \code{i} is the
sorted integer vector of the identified direct parents of variable \code{i}
(possibly empty). Like \code{\link[=lingam_rcd]{lingam_rcd()}}, there is no \code{causal_order}.
\item \code{confounded_pairs}: 2-column integer matrix of the variable pairs
(1-based column positions) suspected to be connected by a UCP or UBP;
0 rows if none.
\item \code{regressor}: label of the regressor used (\code{"gam"} or
\code{"user function"}).
}
}
\description{
A causal discovery method for \strong{nonlinear additive} models that allows
unobserved variables. CAM-UV assumes each observed variable is a
generalized additive function of its parents plus independent noise, and
the causal structure is a DAG. It identifies each variable's direct
parents by scanning variable subsets and testing whether the GAM
regression residual is independent of the candidate parents
(\code{camuv_find_parents()}); variable pairs whose residuals remain dependent
without an identified edge are reported as connected by an \strong{unobserved
causal path} (UCP: a directed path through an unobserved variable) or an
\strong{unobserved backdoor path} (UBP: a common unobserved ancestor), and are
marked \code{NA} in the adjacency matrix rather than oriented.
}
\details{
The three stages mirror the Python implementation: (1)
\code{camuv_get_neighborhoods()} records which raw variable pairs are
dependent; (2) \code{camuv_find_parents()} scans variable subsets of size 2 up
to \code{num_explanatory_vals}, identifying the most sink-like variable of
each subset and re-testing until no new parent is found; (3) remaining
dependent pairs without an identified edge are flagged as UCP/UBP pairs.

Every subset test involves GAM regressions and an HSIC test on \verb{n x n}
Gram matrices, so the method is not recommended for \code{nrow(X)} in the
thousands, and cost grows combinatorially with \code{num_explanatory_vals}.

\code{independence = "fcorr"} is only supported with
\code{num_explanatory_vals = 2} (the default): larger subsets require testing
a residual against several variables jointly, and the F-correlation is
defined for univariate pairs only (the Python implementation breaks on
that combination as well). The HSIC path supports any
\code{num_explanatory_vals} via the multivariate kernel.

At least 6 observations are required (the lower bound of the HSIC
gamma-approximation test).

Unlike most other estimators in this package there is no bootstrap
variant, matching the Python implementation, and no total-effect
estimation (effects are nonlinear).
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  confounded <- generate_camuv_sample(n = 200, seed = 1)
  result <- lingam_camuv(confounded$data)
  print(result)

  # Pairs connected through unobserved variables are left NA
  result$confounded_pairs
}
}
}
\references{
Maeda, T. N. and Shimizu, S. (2021). Causal additive models with
unobserved variables. In Proc. Thirty-Seventh Conference on Uncertainty
in Artificial Intelligence (UAI), PMLR 161: 97-106.
}
