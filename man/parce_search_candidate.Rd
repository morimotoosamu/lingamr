% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_parce.r
\name{parce_search_candidate}
\alias{parce_search_candidate}
\title{Candidate sink variables under prior knowledge (ParceLiNGAM direction)}
\usage{
parce_search_candidate(U, partial_orders)
}
\arguments{
\item{U}{set of currently undetermined variables}

\item{partial_orders}{matrix of (from, to) pairs from \code{\link[=extract_partial_orders]{extract_partial_orders()}}}
}
\value{
candidate index vector (subset of U, or U itself if no candidates remain)
}
\description{
Unlike \code{\link[=search_candidate]{search_candidate()}} (used by top-down DirectLiNGAM), ParceLiNGAM
searches from the sink side, so the filter is simply "exclude variables
that appear as the 'from' side of a known partial order" (they cannot be
a sink because something is known to cause a variable through them...
more precisely: a variable known to \emph{cause} another remaining variable
cannot itself be the next sink).
}
\keyword{internal}
