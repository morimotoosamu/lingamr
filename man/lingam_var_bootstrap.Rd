% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var_bootstrap.r
\name{lingam_var_bootstrap}
\alias{lingam_var_bootstrap}
\title{Bootstrap for VAR-LiNGAM}
\usage{
lingam_var_bootstrap(
  X,
  n_sampling,
  lags = 1L,
  criterion = "bic",
  measure = "pwling",
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  prune = TRUE,
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{X}{numeric matrix or data frame (n_samples x n_features), rows ordered
in time.}

\item{n_sampling}{number of bootstrap iterations (positive integer).}

\item{lags}{maximum lag order. When \code{criterion} is not NULL, the lag is
selected once on the original data and then fixed across all iterations.}

\item{criterion}{lag-selection criterion ("bic", "aic", "hqic", "fpe") or
NULL to use \code{lags} directly.}

\item{measure}{independence measure for \code{\link[=lingam_direct]{lingam_direct()}} ("pwling"/"kernel").}

\item{reg_method}{regression method for the instantaneous matrix.}

\item{lambda}{penalty selection (see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{init_method}{initial-weight method for adaptive LASSO.}

\item{prune}{logical; passed to \code{\link[=lingam_var]{lingam_var()}} on each iteration (default TRUE).}

\item{seed}{random seed (NULL allowed).}

\item{verbose}{whether to print progress (logical).}

\item{parallel}{whether to distribute iterations across cores (logical).}

\item{n_cores}{number of cores (integer or NULL; NULL caps at 2 for safety).}
}
\value{
a \code{VARBootstrapResult} object.
}
\description{
Evaluates the statistical reliability of the estimated time-series DAG by
resampling. Unlike the i.i.d. row resampling used for Direct LiNGAM, this
uses a \strong{residual bootstrap}: the VAR is fitted once on the original data,
the residuals are resampled with replacement, and a new series is rebuilt by
the VAR recursion before re-estimating VAR-LiNGAM on it (this preserves the
autoregressive structure). Port of the Python reference \code{VARLiNGAM.bootstrap}.
}
\details{
Reproducibility follows the same rules as \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}: with
\code{parallel = TRUE}, L'Ecuyer streams via \code{parallel::clusterSetRNGStream()} make
results reproducible for a given \code{seed} and \code{n_cores}, but they do not match
the sequential (\code{parallel = FALSE}) results.
}
\examples{
s <- generate_varlingam_sample(n = 500, seed = 42)

# Fast example: OLS instantaneous structure, no pruning (no glmnet needed)
bs <- lingam_var_bootstrap(s$data,
  n_sampling = 10L, lags = 1, criterion = NULL,
  reg_method = "ols", prune = FALSE, seed = 1, verbose = FALSE
)
get_var_probabilities(bs)
}
