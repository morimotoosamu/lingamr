% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_rcd.r
\name{rcd_is_non_gaussian_all}
\alias{rcd_is_non_gaussian_all}
\title{Non-Gaussianity judgment (Shapiro-Wilk) for a set of columns}
\usage{
rcd_is_non_gaussian_all(Y, cols, shapiro_alpha)
}
\arguments{
\item{Y}{data matrix}

\item{cols}{column indices to test}

\item{shapiro_alpha}{significance level}
}
\value{
TRUE if all columns reject normality (p <= shapiro_alpha)
}
\description{
When \code{n > SHAPIRO_MAX_N}, \code{stats::shapiro.test()} cannot be used directly
(hard cap at 5000), so the deterministic evenly-spaced subsample from
\code{shapiro_subsample()} (defined in \code{R/get_error_independence_p_values.r})
is tested instead, matching the behavior of \code{test_residual_normality()}.
The deterministic thinning keeps results reproducible across calls and
leaves the caller's RNG stream untouched.
}
\keyword{internal}
