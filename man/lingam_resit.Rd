% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_resit.r
\name{lingam_resit}
\alias{lingam_resit}
\title{RESIT causal discovery for nonlinear additive noise models}
\usage{
lingam_resit(X, regressor = "gam", alpha = 0.01, prior_knowledge = NULL)
}
\arguments{
\item{X}{numeric matrix or data frame of observed variables}

\item{regressor}{nonlinear regressor used for all internal regressions.
Either the string \code{"gam"} (default; requires the suggested package
mgcv, and fits a smoothing-spline GAM per regression) or a function
\verb{function(X, y)} that receives a predictor matrix and a response
vector and returns the fitted values as a numeric vector of length
\code{nrow(X)} (the model is only ever evaluated on its own training data,
mirroring \verb{regressor.fit(X, y); regressor.predict(X)} in Python)}

\item{alpha}{significance level of the HSIC independence test used for
edge pruning (default: 0.01; must be non-negative)}

\item{prior_knowledge}{optional prior-knowledge matrix with elements 1
(directed path exists), 0 (no directed path), and -1 or NA (unknown),
with the same \verb{[to, from]} orientation as the adjacency matrix}
}
\value{
An object of class \code{ResitResult} with elements:
\itemize{
\item \code{adjacency_matrix}: (p x p) 0/1 matrix; \code{B[i, j] = 1} means an edge
\code{j -> i} (row = to, col = from). Entries are edge indicators, not
coefficients.
\item \code{causal_order}: estimated causal order (1-based column positions,
source first).
\item \code{regressor}: label of the regressor used (\code{"gam"} or
\code{"user function"}).
}
}
\description{
R port of the RESIT algorithm (Regression with Subsequent Independence
Test) from the Python \code{lingam} package. RESIT assumes a nonlinear
additive noise model \code{x_i = f_i(parents(x_i)) + e_i} and recovers the
causal structure in two phases: (1) the causal order is estimated by
repeatedly detaching the most sink-like variable, i.e. the variable
whose regression residual on all remaining variables is least dependent
on them (smallest HSIC statistic); (2) superfluous parents are pruned by
testing, for each candidate parent, whether the residual regressed on
the other parents is already independent of the parent set (HSIC
p-value greater than \code{alpha}).
}
\details{
Because the model is nonlinear, the returned \code{adjacency_matrix} contains
0/1 edge indicators, \strong{not} connection strengths, and total causal
effects are undefined: the Python implementation's
\code{estimate_total_effect()} (always 0) and
\code{get_error_independence_p_values()} (always a zero matrix) are
intentionally not ported.

Note the direction of \code{alpha}: a parent is removed when the HSIC p-value
exceeds \code{alpha}, so \emph{larger} values of \code{alpha} make the test stricter
about declaring independence and therefore \emph{keep more edges}.

The HSIC test in phase 1 measures dependence between a residual and the
joint (multivariate) kernel of up to \code{ncol(X) - 1} predictors. Following
the Python original, variables are not standardized beforehand; if the
variable scales differ wildly, the largest-scale variable dominates the
kernel distances. Each HSIC call builds n x n Gram matrices, and
\code{O(ncol(X)^2)} regressions and HSIC tests are performed overall, so the
method is not recommended for \code{nrow(X)} in the thousands.

At least 6 observations are required (the lower bound of the HSIC
gamma-approximation test), which is stricter than the linear methods in
this package.

Deviation from the Python original when \code{prior_knowledge} excludes every
remaining sink candidate: the candidate set falls back to all remaining
variables (the original would fail), matching \code{\link[=lingam_parce]{lingam_parce()}}.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  nonlinear <- generate_resit_sample(n = 300, seed = 1)
  result <- lingam_resit(nonlinear$data)
  print(result)
}
}
}
\references{
J. Peters, J. M. Mooij, D. Janzing, B. Schoelkopf. Causal discovery with
continuous additive noise models. Journal of Machine Learning Research,
15: 2009-2053, 2014.
}
