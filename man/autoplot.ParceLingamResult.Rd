% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.r
\name{autoplot.ParceLingamResult}
\alias{autoplot.ParceLingamResult}
\title{Plot the causal graph of a ParceLingamResult with ggplot2}
\usage{
\method{autoplot}{ParceLingamResult}(
  object,
  threshold = 0,
  node_size = 16,
  node_color = "lightblue",
  label_edges = TRUE,
  label_pos = 0.35,
  ...
)
}
\arguments{
\item{object}{Return value of \code{\link[=lingam_parce]{lingam_parce()}} (a \code{ParceLingamResult} object)}

\item{threshold}{Coefficients with an absolute value at or below this are not treated as edges (default: 0)}

\item{node_size}{Node size (default: 16)}

\item{node_color}{Node fill color (default: "lightblue")}

\item{label_edges}{Whether to display coefficient labels on edges (default: TRUE)}

\item{label_pos}{Position of each coefficient label along its edge, as a
fraction from the source (0) to the target (1). The default 0.35 places
labels off-center (toward the source) so labels on crossing edges do not
overlap near the midpoint.}

\item{...}{Unused}
}
\value{
A ggplot object
}
\description{
Draws the estimated causal structure as a ggplot2-based directed graph,
like \code{\link[=autoplot.LingamResult]{autoplot.LingamResult()}}. Variable pairs whose adjacency-matrix
entries are \code{NA} (unresolved order / suspected latent confounding) are
drawn as dashed, arrowless segments.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("igraph", quietly = TRUE)) {
  library(ggplot2)
  dat <- generate_parce_sample(n = 500, seed = 42)
  model <- lingam_parce(dat$data)
  autoplot(model)
}
}
}
