% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_model_fit.r
\name{build_lavaan_model}
\alias{build_lavaan_model}
\title{Build a lavaan model string from an adjacency matrix}
\usage{
build_lavaan_model(B, var_names)
}
\arguments{
\item{B}{adjacency matrix (machine names \verb{x0, x1, ...} expected as
row/column indices; the caller supplies names via \code{var_names})}

\item{var_names}{machine variable names, length \code{ncol(B)}}
}
\value{
character scalar, lavaan model syntax (possibly empty string)
}
\description{
Converts a lingamr-convention adjacency matrix (\code{B[i, j]} = causal
coefficient from j to i) into a lavaan model syntax string. Non-zero
elements become regression paths (\code{xi ~ xj}); \code{NA} elements (used by
e.g. \code{\link[=lingam_parce]{lingam_parce()}} to mark a latent confounder between two variables)
become a residual covariance (\code{xi ~~ xj}) between the two machine-named
variables, which is the standard lavaan idiom equivalent to a two-indicator
latent common cause with one loading fixed (as used by the Python
\code{semopy}-based original).
}
\keyword{internal}
