% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_parce_bootstrap.r
\name{lingam_parce_bootstrap}
\alias{lingam_parce_bootstrap}
\title{Bootstrap for Bottom-Up ParceLiNGAM}
\usage{
lingam_parce_bootstrap(
  X,
  n_sampling,
  prior_knowledge = NULL,
  alpha = 0.1,
  independence = "hsic",
  ind_corr = 0.5,
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL,
  compute_total_effects = TRUE
)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features)}

\item{n_sampling}{Number of bootstrap iterations}

\item{prior_knowledge}{Prior knowledge matrix (NULL allowed)}

\item{alpha}{Significance level, passed to \code{\link[=lingam_parce]{lingam_parce()}}}

\item{independence}{Independence measure, passed to \code{\link[=lingam_parce]{lingam_parce()}}}

\item{ind_corr}{F-correlation rejection threshold, passed to \code{\link[=lingam_parce]{lingam_parce()}}}

\item{reg_method}{Regression method ("ols", "lasso", "adaptive_lasso", "ridge")}

\item{lambda}{Lambda selection ("lambda.min", "lambda.1se", "AIC", "BIC", "oracle")}

\item{init_method}{Method for estimating the initial weights of adaptive LASSO
regression ("ols" or "ridge")}

\item{seed}{Random seed (NULL allowed)}

\item{verbose}{Whether to display progress (logical)}

\item{parallel}{Whether to use parallel processing (logical)}

\item{n_cores}{Number of cores to use (integer, NULL allowed)}

\item{compute_total_effects}{Whether to also estimate total causal effects
for every variable pair on each bootstrap iteration (logical, default \code{TRUE}).}
}
\value{
A \code{BootstrapResult} (list); see \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}} for the
query helpers that operate on it (\code{get_probabilities()},
\code{get_causal_direction_counts()}, \code{get_directed_acyclic_graph_counts()},
\code{get_total_causal_effects()}).
}
\description{
Bootstrap for Bottom-Up ParceLiNGAM
}
\details{
\strong{Total effects are path sums, not regression estimates.} Each
iteration's total-effect matrix is built from \code{\link[=calculate_total_effect]{calculate_total_effect()}}
(summing products of adjacency-matrix coefficients along every directed
path), matching the upstream Python implementation's bootstrap method
(\code{estimate_total_effect2}). If a variable's row in the adjacency matrix
contains \code{NA} (it is part of an unresolved block), all of its outgoing
total effects are set to \code{NA} for that iteration, since its causal
parents cannot be identified.

\strong{\code{NA} (unresolved) edges are treated as absent when aggregating.} Both
the adjacency matrix and the total-effect matrix have \code{NA} replaced by
\code{0} before being stored in the returned \code{BootstrapResult}, matching the
numpy comparison semantics used by the upstream implementation (where
\code{np.abs(nan) > threshold} evaluates to \code{FALSE}). This means, for example,
\code{get_probabilities()} reports the confounded pair's edge probability as
the fraction of resamples in which the order happened to resolve, not as
\code{NA}.

\strong{\code{causal_orders} is not populated} (unlike \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}):
ParceLiNGAM's causal order can include an unresolved block, which does
not fit the fixed-length integer-vector format \code{causal_orders} requires.
As a result, \code{\link[=get_causal_order_stability]{get_causal_order_stability()}} cannot be used with a
\code{BootstrapResult} returned by this function.
}
\examples{
\donttest{
confounded <- generate_parce_sample(n = 500, seed = 1)

bs <- lingam_parce_bootstrap(confounded$data,
  n_sampling = 10L,
  reg_method = "ols",
  seed = 42
)
get_probabilities(bs)
}
}
