% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_rcd_sample.r
\name{generate_rcd_sample}
\alias{generate_rcd_sample}
\title{Generate sample data with a latent confounder (for RCD)}
\usage{
generate_rcd_sample(n = 300L, seed = NULL)
}
\arguments{
\item{n}{number of samples (default: 300)}

\item{seed}{random seed (default: NULL, i.e. do not reset the RNG state)}
}
\value{
list with four elements:
\itemize{
\item \code{data}: data.frame of the 6 observed variables (\code{x0}-\code{x5}).
\item \code{adjacency_matrix}: the true 6x6 adjacency matrix among the observed
variables, following the \code{m[to, from]} convention. The \code{x2}-\code{x4} entries
(which share the latent confounder \code{x6} and have no direct edge between
them) are \code{NA}, matching the convention used by \code{\link[=lingam_rcd]{lingam_rcd()}}.
\item \code{ancestors_list}: the true ancestor sets (1-based column positions),
usable as a test oracle for \code{\link[=lingam_rcd]{lingam_rcd()}}'s \code{ancestors_list}.
\item \code{confounded_pair}: 1-based column positions of \code{x2} and \code{x4} (the pair
sharing the latent confounder).
}
}
\description{
Generates the 7-variable model used in the RCD tutorial, where \code{x6} is an
unobserved (latent) common cause of \code{x2} and \code{x4}. Only \code{x0}-\code{x5} are
returned as observed data; \code{x6} is not included.
}
\details{
The data-generating process (all error terms \code{e()} are super-Gaussian,
\code{rnorm(n, 0, 0.5)^3}):
\preformatted{
x5 ~ e();  x6 (latent) ~ e()
x1 = 0.6 * x5 + e()
x3 = 0.5 * x5 + e()
x0 = 1.0 * x1 + 1.0 * x3 + e()
x2 = 0.8 * x0 - 0.6 * x6 + e()
x4 = 1.0 * x0 - 0.5 * x6 + e()
}
}
\examples{
confounded <- generate_rcd_sample(n = 300, seed = 1)
head(confounded$data)
confounded$adjacency_matrix
confounded$ancestors_list
confounded$confounded_pair
}
