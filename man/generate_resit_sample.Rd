% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_resit_sample.r
\name{generate_resit_sample}
\alias{generate_resit_sample}
\title{Generate sample data from a nonlinear additive noise model (for RESIT)}
\usage{
generate_resit_sample(n = 300L, seed = NULL)
}
\arguments{
\item{n}{number of samples (default: 300)}

\item{seed}{random seed (default: NULL, i.e. do not reset the RNG state)}
}
\value{
list with three elements:
\itemize{
\item \code{data}: data.frame of the 4 observed variables (\code{x0}-\code{x3}).
\item \code{adjacency_matrix}: the true 4x4 adjacency matrix following the
\code{m[to, from]} convention. Entries are 0/1 edge indicators (not
coefficients), directly comparable to the output of \code{\link[=lingam_resit]{lingam_resit()}}.
\item \code{causal_order}: the true causal order (1-based column positions,
source first).
}
}
\description{
Generates a 4-variable nonlinear SEM whose structure can only be
recovered by a nonlinear method such as \code{\link[=lingam_resit]{lingam_resit()}}; linear
LiNGAM variants are not expected to work on this data.
}
\details{
The data-generating process (all error terms \code{e()} are
\code{runif(n, -0.5, 0.5)}):
\preformatted{
x0 ~ runif(-1, 1)
x1 = 3.0 * x0^2 + e()
x2 = 2.0 * tanh(x1) + 0.8 * x0^3 + e()
x3 = 1.5 * sin(x2) + e()
}
}
\examples{
nonlinear <- generate_resit_sample(n = 300, seed = 1)
head(nonlinear$data)
nonlinear$adjacency_matrix
}
