% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.LiMResult}
\alias{tidy.LiMResult}
\title{Convert a LiMResult to a tidy data.frame}
\usage{
\method{tidy}{LiMResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_lim]{lingam_lim()}} (a \code{LiMResult} object)}

\item{threshold}{Coefficients with an absolute value at or below this are not
treated as edges (default: 0)}

\item{...}{Unused}
}
\value{
data.frame(from, to, estimate)
}
\description{
Converts the estimated adjacency matrix of a LiM model into a long-format
data.frame with one edge per row, exactly like \code{\link[=tidy.LingamResult]{tidy.LingamResult()}}.
}
\examples{
set.seed(1)
dat <- generate_lim_sample(n = 300)
model <- lingam_lim(dat$data, is_continuous = dat$is_continuous)
tidy(model)
}
