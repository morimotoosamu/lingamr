% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_with_imputation.r
\name{bootstrap_with_imputation}
\alias{bootstrap_with_imputation}
\title{Bootstrap with Multiple Imputation for Direct LiNGAM}
\usage{
bootstrap_with_imputation(
  X,
  n_sampling,
  n_repeats = 10L,
  imputer = NULL,
  cd_fit = NULL,
  prior_knowledge = NULL,
  apply_prior_knowledge_softly = FALSE,
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{X}{A numeric matrix or data frame (n_samples x n_features). May
contain \code{NA}. If \code{X} has no missing values, a warning suggests using
\code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}} instead, and estimation proceeds anyway.}

\item{n_sampling}{Number of bootstrap iterations (positive integer)}

\item{n_repeats}{Number of imputed datasets generated per bootstrap sample
(positive integer, default \code{10L}). Ignored when a custom \code{imputer} is
supplied; the number of datasets it returns is used instead.}

\item{imputer}{\code{NULL}, or a \verb{function(X_boot)} returning a list of
complete (no-NA) numeric matrices, each with the same dimensions as
\code{X_boot}. Defaults to multiple imputation via \code{mice::mice(method = "norm")}.}

\item{cd_fit}{\code{NULL}, or a \verb{function(X_list)} returning
\verb{list(causal_order = <integer vector, 1-based permutation>, adjacency_matrices = <list of p x p matrices, one per element of X_list>)}.
Defaults to joint estimation via \code{\link[=lingam_multi_group]{lingam_multi_group()}}.}

\item{prior_knowledge}{Prior knowledge matrix (NULL allowed). Only used
when \code{cd_fit = NULL}; a warning is issued if supplied together with a
custom \code{cd_fit}.}

\item{apply_prior_knowledge_softly}{Apply prior knowledge softly (logical).
Same restriction as \code{prior_knowledge}.}

\item{seed}{Random seed (NULL allowed). In sequential mode it is set once
before the bootstrap loop and governs both the resampling and (via the
global RNG) \code{mice}'s imputation; in parallel mode it seeds the L'Ecuyer
parallel random-number streams (see Details).}

\item{verbose}{Whether to display progress (logical)}

\item{parallel}{Whether to use parallel processing (logical)}

\item{n_cores}{Number of cores to use (integer, NULL allowed). When \code{NULL},
the number of cores is limited to a maximum of 2 for safety. Ignored when
\code{parallel = FALSE}.}
}
\value{
An \code{ImputationBootstrapResult} (list) containing:
\itemize{
\item \code{causal_orders}: \code{n_sampling} x \code{p} integer matrix (1-based causal order per iteration).
\item \code{adjacency_matrices}: \code{array(n_sampling, n_repeats, p, p)}; \verb{[, , i, j]}
follows the lingamr convention (\code{B[i, j]} = coefficient of j -> i).
\item \code{resampled_indices}: \code{n_sampling} x \code{n} integer matrix of resampled row indices.
\item \code{imputation_results}: \code{array(n_sampling, n_repeats, n, p)}; non-\code{NA} only
at positions that were missing in that iteration's bootstrap resample.
}
}
\description{
Causal discovery on data containing missing values (NA). Each bootstrap
resample (drawn with replacement, missing values retained) is multiply
imputed into \code{n_repeats} complete datasets, and a common causal structure
is jointly estimated across those datasets with \code{\link[=lingam_multi_group]{lingam_multi_group()}}
(Shimizu 2012), treating the imputed copies as "groups" sharing one causal
order. R port of the Python \code{lingam.tools.bootstrap_with_imputation()}.
}
\details{
\strong{Procedure:} for each of \code{n_sampling} iterations, (1) resample \code{X} with
replacement (missing values are retained), (2) impute the resample into
\code{n_repeats} complete datasets, (3) jointly estimate one causal structure
shared by all \code{n_repeats} datasets with \code{\link[=lingam_multi_group]{lingam_multi_group()}}. This
assumes the same causal structure underlies every imputed copy.

\strong{Default imputer.} \code{mice::mice(method = "norm")} (Bayesian linear
regression, multiple imputation by chained equations) is the closest
standard R analogue of the upstream Python default
(\code{IterativeImputer(sample_posterior = TRUE)}). The two do not produce
numerically identical imputations.

\strong{Custom \code{imputer} / \code{cd_fit}.} Supply your own imputation or
causal-discovery routine by passing a function with the signature
described above; the return value is validated and a descriptive error is
raised on violation. This replaces the abstract base classes
(\code{BaseMultipleImputation}, \code{BaseMultiGroupCDModel}) of the Python original.

\strong{Downstream analysis.} The result's shape (an extra \code{n_repeats}
dimension for \code{adjacency_matrices} and \code{imputation_results}) differs from
\code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}'s \code{BootstrapResult}, so it cannot be passed
directly to \code{\link[=get_probabilities]{get_probabilities()}} etc. Use \code{\link[=as_bootstrap_result]{as_bootstrap_result()}} to
collapse the \code{n_repeats} dimension (aggregating by median or mean) into a
\code{BootstrapResult}.

\strong{On iteration failures:} each iteration is wrapped in \code{tryCatch()}; a
failing iteration (e.g. \code{mice} fails to converge on a particular resample)
is skipped with a warning, and only if every iteration fails is an error
raised, mirroring \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}.

\strong{On reproducibility:} During parallel execution, L'Ecuyer parallel random
number streams via \code{parallel::clusterSetRNGStream()} are used. Results are
reproducible given the same \code{seed} and same \code{n_cores}, but they do not
numerically match the results of sequential execution (\code{parallel = FALSE}).
If you need results that exactly match the sequential version, use
\code{parallel = FALSE}. The 'mice' imputation draws from each worker's global
RNG, so it is covered by the same stream mechanism.
}
\examples{
set.seed(1)
sample6 <- generate_lingam_sample_6(n = 300, seed = 1)
X <- sample6$data
X$x5[sample.int(nrow(X), size = round(0.1 * nrow(X)))] <- NA # MCAR 10\% on x5

\donttest{
if (requireNamespace("mice", quietly = TRUE)) {
  res <- bootstrap_with_imputation(X,
    n_sampling = 5L, n_repeats = 3L, seed = 42, verbose = FALSE
  )
  print(res)

  # Collapse the n_repeats dimension to reuse the existing bootstrap tooling
  bs <- as_bootstrap_result(res, aggregate = "median")
  get_probabilities(bs)
}
}
}
