% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma.r
\name{select_varma_order}
\alias{select_varma_order}
\title{Select the VARMA order by information criterion}
\usage{
select_varma_order(X, max_p, max_q, criterion = "bic")
}
\arguments{
\item{X}{numeric matrix (n_samples x n_features)}

\item{max_p}{maximum AR order to consider}

\item{max_q}{maximum MA order to consider}

\item{criterion}{"bic", "aic", or "hqic"}
}
\value{
the selected order \code{c(p, q)} (integer vector)
}
\description{
All candidate orders \verb{(p, q)} in \verb{0:max_p x 0:max_q} (excluding \verb{(0, 0)})
are compared on a \strong{common sample}: the stage-1 long VAR is estimated once
with \code{h} derived from \verb{(max_p, max_q)}, and every stage-2 candidate
regression uses the same window t = h + max_q + 1 .. n, so the criteria are
comparable across orders (same rationale as \code{\link[=select_var_lag]{select_var_lag()}}).
}
\keyword{internal}
