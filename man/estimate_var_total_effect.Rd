% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var_total_effect.r
\name{estimate_var_total_effect}
\alias{estimate_var_total_effect}
\title{Estimate a total causal effect in a VAR-LiNGAM model}
\usage{
estimate_var_total_effect(X, result, from_index, to_index, from_lag = 0)
}
\arguments{
\item{X}{original data (matrix or data frame), rows ordered in time}

\item{result}{a \code{VARLiNGAMResult} from \code{\link[=lingam_var]{lingam_var()}}}

\item{from_index}{source variable (1-based index or variable name)}

\item{to_index}{destination variable (1-based index or variable name)}

\item{from_lag}{lag of the source variable (0 = current time, default)}
}
\value{
the estimated total effect (scalar)
}
\description{
Estimates the total causal effect from \code{from_index} (optionally at lag
\code{from_lag}) to \code{to_index} (at the current time) using the fitted VAR-LiNGAM
model. Port of the Python reference \code{estimate_total_effect}: the destination
variable is regressed on the source variable together with the source's
parents (a back-door adjustment), and the source's coefficient is returned.
}
\examples{
sample <- generate_varlingam_sample(n = 1000, seed = 42)
model <- lingam_var(sample$data, lags = 1, reg_method = "ols", prune = FALSE)

# total effect of x0 (current) on x2 (current)
estimate_var_total_effect(sample$data, model, from_index = 1, to_index = 3)
}
