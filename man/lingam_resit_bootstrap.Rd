% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_resit_bootstrap.r
\name{lingam_resit_bootstrap}
\alias{lingam_resit_bootstrap}
\title{Bootstrap for RESIT}
\usage{
lingam_resit_bootstrap(
  X,
  n_sampling,
  regressor = "gam",
  alpha = 0.01,
  prior_knowledge = NULL,
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features)}

\item{n_sampling}{Number of bootstrap iterations}

\item{regressor}{Nonlinear regressor, passed to \code{\link[=lingam_resit]{lingam_resit()}}}

\item{alpha}{Significance level of the HSIC pruning test, passed to
\code{\link[=lingam_resit]{lingam_resit()}}}

\item{prior_knowledge}{Prior-knowledge matrix, passed to \code{\link[=lingam_resit]{lingam_resit()}}}

\item{seed}{Random seed (NULL allowed)}

\item{verbose}{Whether to display progress (logical)}

\item{parallel}{Whether to use parallel processing (logical)}

\item{n_cores}{Number of cores to use (integer, NULL allowed)}
}
\value{
A \code{BootstrapResult} (list); see \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}} for the
query helpers that operate on it (\code{get_probabilities()},
\code{get_causal_direction_counts()}, \code{get_directed_acyclic_graph_counts()},
\code{get_causal_order_stability()}).
}
\description{
Bootstrap for RESIT
}
\details{
\strong{\code{total_effects} is always \code{NULL}}: RESIT is a nonlinear method for
which total causal effects are undefined, so there is no
\code{compute_total_effects} argument and \code{\link[=get_total_causal_effects]{get_total_causal_effects()}} raises
its usual "no total effects" error. (The Python implementation instead
stores an all-zero total-effects array; storing nothing is deliberate, so
the zeros cannot be mistaken for estimated effects.)

\strong{\code{causal_orders} is populated} (RESIT estimates a full causal order),
so \code{\link[=get_causal_order_stability]{get_causal_order_stability()}} works with the returned object.

Each iteration runs \code{O(ncol(X)^2)} nonlinear regressions plus HSIC tests,
so a bootstrap is substantially slower than the linear variants; keep
\code{n_sampling} modest. When \code{parallel = TRUE}, a user-supplied \code{regressor}
function is serialized to the PSOCK workers; any package it uses must be
referenced with the \code{pkg::fun} form inside the function body.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  nonlinear <- generate_resit_sample(n = 300, seed = 1)

  bs <- lingam_resit_bootstrap(nonlinear$data,
    n_sampling = 3L,
    seed = 42
  )
  get_probabilities(bs)
}
}
}
