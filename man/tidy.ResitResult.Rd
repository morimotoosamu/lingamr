% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.ResitResult}
\alias{tidy.ResitResult}
\title{Convert a ResitResult to a tidy data.frame}
\usage{
\method{tidy}{ResitResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_resit]{lingam_resit()}} (a \code{ResitResult} object)}

\item{threshold}{Kept for interface consistency with the other \code{tidy()}
methods (default: 0); with 0/1 entries any value in \verb{[0, 1)} returns
all edges}

\item{...}{Unused}
}
\value{
data.frame(from, to, estimate)
}
\description{
Converts the estimated 0/1 adjacency matrix into a long-format data.frame
with one edge per row, like \code{\link[=tidy.LingamResult]{tidy.LingamResult()}}. Because RESIT is a
nonlinear method, \code{estimate} is always 1 (an edge indicator, not a
coefficient).
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  nonlinear <- generate_resit_sample(n = 300, seed = 1)
  model <- lingam_resit(nonlinear$data)
  tidy(model)
}
}
}
