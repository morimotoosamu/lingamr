% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_camuv_sample.r
\name{generate_camuv_sample}
\alias{generate_camuv_sample}
\title{Generate sample data with unobserved variables (for CAM-UV)}
\usage{
generate_camuv_sample(n = 500L, seed = NULL)
}
\arguments{
\item{n}{number of samples (default: 500)}

\item{seed}{random seed (default: NULL, i.e. do not reset the RNG state)}
}
\value{
list with three elements:
\itemize{
\item \code{data}: data.frame of the 6 observed variables (\code{x0}-\code{x5}).
\item \code{adjacency_matrix}: the true 6x6 adjacency matrix among the observed
variables, following the \code{m[to, from]} convention. Entries are 0/1
edge indicators (the causal functions are nonlinear), directly
comparable to the output of \code{\link[=lingam_camuv]{lingam_camuv()}}. The \code{x3}-\code{x4} (UBP) and
\code{x2}-\code{x5} (UCP) entries are \code{NA}, matching the convention used by
\code{\link[=lingam_camuv]{lingam_camuv()}}.
\item \code{confounded_pairs}: 2-column integer matrix of the variable pairs
(1-based column positions) connected through an unobserved variable,
usable as a test oracle for \code{\link[=lingam_camuv]{lingam_camuv()}}'s \code{confounded_pairs}.
}
}
\description{
Generates a 6-variable nonlinear additive model with two \emph{unobserved}
variables, following the structure of the CAM-UV tutorial of the Python
\code{lingam} package: \code{u1} is an unobserved common cause of \code{x3} and \code{x4}
(an unobserved backdoor path, UBP), and \code{u2} is an unobserved
intermediate variable on the path from \code{x2} to \code{x5} (an unobserved
causal path, UCP). Only \code{x0}-\code{x5} are returned as observed data.
}
\details{
The data-generating process (all error terms \code{e()} are
\code{runif(n, -2.5, 2.5)}; the tutorial's per-call random constants are
replaced by the fixed constants below for reproducibility; each column
is standardized before being used as a cause, as in the tutorial):
\preformatted{
u1 (latent) ~ e();  x0..x5 ~ e()
x3 = x3 + (u1 + 1.5)^2
x4 = x4 + (u1 + 1.2)^2
x1 = x1 + (x0 + 1.2)^2
x3 = x3 + (x0 - 1.5)^2
x4 = x4 + (x2 + 1.0)^2
u2 (latent) = (x2 - 1.2)^2 + e()
x5 = x5 + (u2 + 1.5)^2
}
}
\examples{
confounded <- generate_camuv_sample(n = 200, seed = 1)
head(confounded$data)
confounded$adjacency_matrix
confounded$confounded_pairs
}
