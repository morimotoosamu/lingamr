% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_bootstrap.r
\name{setup_cluster_worker}
\alias{setup_cluster_worker}
\title{Make this package's functions available on parallel cluster workers}
\usage{
setup_cluster_worker(cl, fun)
}
\arguments{
\item{cl}{A \code{parallel} cluster object (from \code{parallel::makePSOCKcluster()}).}

\item{fun}{A function belonging to the package whose namespace environment
should be used as the fallback export source (e.g. \code{lingam_direct},
\code{lingam_rcd}, \code{lingam_parce}, \code{lingam_multi_group}).}
}
\value{
\code{NULL}, invisibly. Called for the side effect of preparing \code{cl}.
}
\description{
Used by all \verb{*_bootstrap()} functions before dispatching iterations via
\code{parallel::parLapply()}. Tries to have each worker \code{library()} the
installed package; if that is not possible (e.g. during
\code{devtools::load_all()} development, where the package is not installed),
falls back to exporting every object in the namespace environment of a
representative function from the algorithm being bootstrapped.
}
\keyword{internal}
