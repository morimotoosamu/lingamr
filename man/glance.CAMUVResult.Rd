% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{glance.CAMUVResult}
\alias{glance.CAMUVResult}
\title{Get a one-row summary of a CAMUVResult}
\usage{
\method{glance}{CAMUVResult}(x, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_camuv]{lingam_camuv()}} (a \code{CAMUVResult} object)}

\item{...}{Unused}
}
\value{
A one-row data.frame(n_variables, n_edges, n_confounded_pairs,
regressor)
}
\description{
Like \code{\link[=glance.LingamResult]{glance.LingamResult()}}, but without a causal order (CAM-UV does not
estimate one). \code{n_edges} counts non-\code{NA} edges only, and
\code{n_confounded_pairs} counts the variable pairs whose adjacency-matrix
entries are \code{NA} (suspected unobserved causal/backdoor path).
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  confounded <- generate_camuv_sample(n = 200, seed = 1)
  model <- lingam_camuv(confounded$data)
  glance(model)
}
}
}
