% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_rcd_bootstrap.r
\name{lingam_rcd_bootstrap}
\alias{lingam_rcd_bootstrap}
\title{Bootstrap for RCD}
\usage{
lingam_rcd_bootstrap(
  X,
  n_sampling,
  max_explanatory_num = 2L,
  cor_alpha = 0.01,
  ind_alpha = 0.01,
  shapiro_alpha = 0.01,
  MLHSICR = FALSE,
  independence = "hsic",
  ind_corr = 0.5,
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL,
  compute_total_effects = TRUE
)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features)}

\item{n_sampling}{Number of bootstrap iterations}

\item{max_explanatory_num}{Maximum number of explanatory variables, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{cor_alpha}{Significance level for correlation tests, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{ind_alpha}{Significance level for the HSIC independence test, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{shapiro_alpha}{Significance level for the non-Gaussianity test, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{MLHSICR}{Whether to use MLHSICR regression, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{independence}{Independence measure, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{ind_corr}{F-correlation rejection threshold, passed to \code{\link[=lingam_rcd]{lingam_rcd()}}}

\item{seed}{Random seed (NULL allowed)}

\item{verbose}{Whether to display progress (logical)}

\item{parallel}{Whether to use parallel processing (logical)}

\item{n_cores}{Number of cores to use (integer, NULL allowed)}

\item{compute_total_effects}{Whether to also estimate total causal effects
for every ancestor pair on each bootstrap iteration (logical, default \code{TRUE}).}
}
\value{
A \code{BootstrapResult} (list); see \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}} for the
query helpers that operate on it (\code{get_probabilities()},
\code{get_causal_direction_counts()}, \code{get_directed_acyclic_graph_counts()},
\code{get_total_causal_effects()}).
}
\description{
Bootstrap for RCD
}
\details{
\strong{Total effects are computed only for ancestor pairs}, driven by each
iteration's \code{ancestors_list} (unlike \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}} and
\code{\link[=lingam_parce_bootstrap]{lingam_parce_bootstrap()}}, which loop over all variable pairs). For a
pair \verb{(from, to)} with \code{from} in \code{to}'s ancestor set, the total effect is
obtained via \code{\link[=calculate_total_effect]{calculate_total_effect()}} (summing products of
adjacency-matrix coefficients along every directed path). If \code{from}'s row
in the adjacency matrix contains \code{NA} (it shares a latent confounder with
some other variable), the effect is set to \code{NA} for that iteration.

\strong{\code{NA} (confounded) edges are treated as absent when aggregating.} Both
the adjacency matrix and the total-effect matrix have \code{NA} replaced by
\code{0} before being stored in the returned \code{BootstrapResult}, matching the
policy used by \code{\link[=lingam_parce_bootstrap]{lingam_parce_bootstrap()}}.

\strong{\code{causal_orders} is not populated}: RCD has no causal order (see
\code{\link[=lingam_rcd]{lingam_rcd()}}). As a result, \code{\link[=get_causal_order_stability]{get_causal_order_stability()}} cannot be
used with a \code{BootstrapResult} returned by this function.

RCD's \code{fit} step is HSIC-heavy and can be slow per iteration, especially
with \code{MLHSICR = TRUE}; keep \code{n_sampling} modest in examples.
}
\examples{
confounded <- generate_rcd_sample(n = 300, seed = 1)

bs <- lingam_rcd_bootstrap(confounded$data,
  n_sampling = 5L,
  seed = 42
)
get_probabilities(bs)
}
