% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.MultiGroupBootstrapResult}
\alias{tidy.MultiGroupBootstrapResult}
\title{Convert a MultiGroupBootstrapResult to a tidy data.frame}
\usage{
\method{tidy}{MultiGroupBootstrapResult}(x, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_multi_group_bootstrap]{lingam_multi_group_bootstrap()}}
(a \code{MultiGroupBootstrapResult} object)}

\item{...}{Arguments passed to \code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}}
(such as \code{n_directions}, \code{min_causal_effect}, \code{split_by_causal_effect_sign})}
}
\value{
data.frame (group, from, to, count, proportion, ...)
}
\description{
Stacks each group's causal direction counts (via
\code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}}) into a single data.frame with a \code{group}
column in front. Arguments for \code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}} can be
passed through \code{...}.
}
\examples{
mg <- generate_multi_group_sample()
bs <- lingam_multi_group_bootstrap(mg$data_list,
  n_sampling = 10L, reg_method = "ols", seed = 42
)
tidy(bs)
}
