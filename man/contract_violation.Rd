% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_with_imputation.r
\name{contract_violation}
\alias{contract_violation}
\title{Signal a hook contract violation (imputer / cd_fit return value)}
\usage{
contract_violation(msg)
}
\arguments{
\item{msg}{Error message}
}
\description{
A plain error, but always called from outside the per-iteration
\code{tryCatch()} in \code{\link[=bootstrap_with_imputation]{bootstrap_with_imputation()}} (see \code{validate_imputer_output()}
/ \code{validate_cd_fit_output()} call sites there), so that a programming error
in a user-supplied hook aborts the whole call immediately instead of being
swallowed as a per-iteration stochastic estimation failure.
}
\keyword{internal}
