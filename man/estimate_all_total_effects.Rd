% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_total_effect.r
\name{estimate_all_total_effects}
\alias{estimate_all_total_effects}
\title{全変数間の総合因果効果を一括推定}
\usage{
estimate_all_total_effects(X, lingam_result)
}
\arguments{
\item{X}{数値行列 (n_samples x n_features)。}

\item{lingam_result}{\code{direct_lingam()} 関数の返り値（隣接行列 \code{adjacency_matrix} と因果順序 \code{causal_order} を含むリスト）。}
}
\value{
総合因果効果の行列。行が結果変数、列が原因変数を表します。
}
\description{
指定されたデータと Direct LiNGAM の推定結果（隣接行列と因果順序）を用いて、
変数間の全ペアの総合因果効果（Total Effects）を、共分散行列を用いて一括で推定します。
}
\details{
各原因変数について、その変数の親（直接の原因）と自分自身を説明変数とし、
因果順序でそれより下流にあるすべての変数を目的変数として、OLS（最小二乗法）による回帰係数を一括計算します。
この手法は全データを繰り返し回帰計算するよりも、共分散行列を用いた行列演算によって高速に動作します。
}
