% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{glance.ParceLingamResult}
\alias{glance.ParceLingamResult}
\title{Get a one-row summary of a ParceLingamResult}
\usage{
\method{glance}{ParceLingamResult}(x, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_parce]{lingam_parce()}} (a \code{ParceLingamResult} object)}

\item{...}{Unused}
}
\value{
A one-row data.frame(n_variables, n_edges, n_na_entries, causal_order)
}
\description{
Like \code{\link[=glance.LingamResult]{glance.LingamResult()}}. \code{n_edges} counts non-\code{NA} edges only, and
\code{n_na_entries} counts the adjacency-matrix entries left \code{NA} (unresolved
order / suspected latent confounding). Unresolved blocks in the causal
order are shown in parentheses, as in the print method.
}
\examples{
dat <- generate_parce_sample(n = 500, seed = 42)
model <- lingam_parce(dat$data)
glance(model)
}
