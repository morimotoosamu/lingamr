% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_high_dim.r
\name{lingam_high_dim}
\alias{lingam_high_dim}
\title{High-Dimensional Direct LiNGAM}
\usage{
lingam_high_dim(X, J = 3L, K = 4L, alpha = 0.5, estimate_adj_mat = TRUE)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features), data frame or matrix}

\item{J}{Assumed largest in-degree (single integer, must be >= 3)}

\item{K}{Degree of the moment used to measure non-Gaussianity (single
integer, must be >= 1)}

\item{alpha}{Cutoff coefficient for pruning away false parents (single
numeric value in \verb{[0, 1]})}

\item{estimate_adj_mat}{Whether to estimate the adjacency matrix (single
logical value). If \code{FALSE}, causal-order search still runs but
\code{adjacency_matrix} is returned as an NA-filled matrix (not \code{NULL}, so
downstream S3 methods keep working).}
}
\value{
A \code{LingamResult} object (list), the same class returned by
\code{\link[=lingam_direct]{lingam_direct()}}, containing:
\itemize{
\item \code{adjacency_matrix}: adjacency matrix B (n_features x n_features).
\strong{Convention: \code{B[i, j]} is the causal coefficient from variable j to
variable i (j -> i).} Zero elements indicate no causal relationship.
All-\code{NA} when \code{estimate_adj_mat = FALSE}.
\item \code{causal_order}: estimated causal order (integer vector of 1-based
indices). Earlier elements are more upstream.
}
}
\description{
A variant of Direct LiNGAM for high-dimensional data (large \code{p}, or
\code{p > n}). Causal order search is based on moment statistics of
non-Gaussianity rather than pairwise independence measures, which is
considerably faster for many variables. Unlike \code{\link[=lingam_direct]{lingam_direct()}}, the
algorithm is deterministic (no random restarts).
}
\details{
When \code{n_samples <= n_features}, the adjacency matrix cannot be estimated
with the usual BIC-based Adaptive LASSO, so this function falls back to a
cross-validated LASSO (\code{glmnet::cv.glmnet}) after emitting a warning.
The upstream Python implementation uses \code{LassoLarsCV} for this fallback;
\code{cv.glmnet} follows the same cross-validation design but is not
numerically identical (different solver: coordinate descent vs. LARS).

The pruning statistic used during causal-order search (Wang & Drton 2020)
is the minimum of a conditional non-Gaussianity statistic over every
size-appropriate conditioning subset. The upstream Python implementation
(cdt15/lingam) has a \code{return} statement mis-indented inside the loop over
conditioning subsets, causing it to only ever evaluate the first subset.
This R implementation intentionally does not replicate that bug and
evaluates every subset, so \code{causal_order} and \code{adjacency_matrix} are not
numerically identical to the upstream Python package.
}
\examples{
sample <- generate_lingam_sample_6(n = 300, seed = 1)
result <- lingam_high_dim(sample$data)
result$causal_order
round(result$adjacency_matrix, 3)

\donttest{
if (requireNamespace("glmnet", quietly = TRUE)) {
  # n <= p: falls back to cross-validated LASSO with a warning
  wide_sample <- generate_lingam_large_sample(p = 30, n = 25, seed = 1)
  wide_result <- lingam_high_dim(wide_sample$data)
  wide_result$causal_order
}
}
}
\references{
Wang, Y. S. and Drton, M. (2020). High-dimensional causal discovery under
non-Gaussianity. Biometrika, 107(1), 41-59.
}
