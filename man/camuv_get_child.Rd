% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_camuv.r
\name{camuv_get_child}
\alias{camuv_get_child}
\title{Select the most sink-like child within a variable set}
\usage{
camuv_get_child(
  X,
  vars,
  P,
  N,
  Y,
  pk_forbidden,
  independence,
  alpha,
  ind_corr,
  reg_fn
)
}
\arguments{
\item{X}{data matrix}

\item{vars}{variable subset (integer vector)}

\item{P}{current parent list}

\item{N}{neighborhood list}

\item{Y}{current residual matrix}

\item{pk_forbidden}{forbidden-cause list, or NULL}

\item{independence}{"hsic" or "fcorr"}

\item{alpha}{significance level (hsic only)}

\item{ind_corr}{rejection threshold (fcorr only)}

\item{reg_fn}{regressor function}
}
\value{
list(child = index or NA, independent = logical); \code{independent}
reports whether the winning candidate's dependence value clears the
configured threshold (alpha / ind_corr)
}
\description{
Faithful port of \code{CAMUV._get_child()}. For each candidate child, the
child is regressed on the candidate parents plus its already-identified
parents, and the residual is tested against the \emph{residual matrix} columns
of the candidate parents. The candidate whose residual is most
independent wins; \code{prev_independence} starts at 0 for hsic (p-value
scale, higher is more independent) and at 1 for fcorr (lower is more
independent), and each accepted candidate raises the bar for the next.
}
\keyword{internal}
