% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_parce.r
\name{lingam_parce}
\alias{lingam_parce}
\title{Bottom-Up ParceLiNGAM}
\usage{
lingam_parce(
  X,
  alpha = 0.1,
  prior_knowledge = NULL,
  independence = "hsic",
  ind_corr = 0.5,
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols"
)
}
\arguments{
\item{X}{Numeric matrix (n_samples x n_features), data frame or matrix}

\item{alpha}{Significance level for the independence test. \code{alpha = 0}
disables rejection entirely, so a full causal order is always returned
(equivalent in spirit to \code{\link[=lingam_direct]{lingam_direct()}}, but using the ParceLiNGAM
sink-search direction and regression). Must be non-negative.}

\item{prior_knowledge}{Prior knowledge matrix (n_features x n_features) or NULL.
0: no directed path from x_i to x_j
1: directed path from x_i to x_j
-1: unknown}

\item{independence}{Independence measure used for the ordering search:
"hsic" (default) uses the HSIC gamma-approximation test combined across
explanatory variables via Fisher's method; "fcorr" uses the F-correlation
(kernel canonical correlation) and rejects based on \code{ind_corr} instead
of a p-value.}

\item{ind_corr}{Threshold on the F-correlation value used only when
\code{independence = "fcorr"}: a candidate is rejected once the largest
F-correlation against any explanatory variable is at or above this
value. Must be non-negative. Ignored when \code{independence = "hsic"}.}

\item{reg_method}{Regression method for adjacency matrix estimation.
"ols": ordinary least squares,
"lasso": LASSO regression,
"adaptive_lasso": adaptive LASSO regression (default, matches the
upstream Python implementation's \code{predict_adaptive_lasso}),
"ridge": Ridge regression.}

\item{lambda}{LASSO penalty (lambda) selection. Same options as
\code{\link[=lingam_direct]{lingam_direct()}}: "lambda.min", "lambda.1se", "AIC", "BIC" (default),
"oracle" (adaptive LASSO only).}

\item{init_method}{Method for estimating the initial weights of adaptive
LASSO regression ("ols" (default) or "ridge").}
}
\value{
A \code{ParceLingamResult} object (list) containing:
\itemize{
\item \code{adjacency_matrix}: adjacency matrix B (n_features x n_features).
\strong{Convention: \code{B[i, j]} is the causal coefficient from variable j to
variable i (j -> i)}, same as \code{\link[=lingam_direct]{lingam_direct()}}. Entries between two
variables that ended up in the same unresolved block are \code{NA}.
\item \code{causal_order}: a list of integer vectors. Elements of length 1 are
variables with a fully resolved position; an element of length > 1 (at
most one, always first) is the unresolved block. Earlier elements are
more upstream.
\item \code{p_values}: independence-test p-values (or F-correlation values, for
\code{independence = "fcorr"}) for each step that successfully placed a
variable, in the order variables were placed (diagnostic only).
\item \code{independence}: the independence measure used.
}
}
\description{
A causal ordering method robust against latent confounders. Unlike
\code{\link[=lingam_direct]{lingam_direct()}}, which always returns a full causal order, this
algorithm searches from the sink (most downstream) side and stops as soon
as an independence test is rejected. Variables it could not order are
returned together as a single "unresolved block" (suspected to share a
latent confounder), and the corresponding entries of the adjacency matrix
are set to \code{NA} rather than estimated.
}
\details{
Because HSIC forms full n x n Gram matrices, it is O(n^2) per test; avoid
very large \code{n} (beyond a few thousand) with \code{independence = "hsic"}.

\code{independence = "fcorr"} rejects based on the raw F-correlation value
(\code{ind_corr}), not a p-value, so it is not directly comparable to \code{alpha}.

\code{\link[=get_error_independence_p_values_parce]{get_error_independence_p_values_parce()}} uses the HSIC test rather than
the correlation-based test used by \code{\link[=get_error_independence_p_values]{get_error_independence_p_values()}}
for \code{LingamResult} objects.

\code{\link[=lingam_parce_bootstrap]{lingam_parce_bootstrap()}} treats \code{NA} (unresolved) edges as absent when
aggregating, and does not support \code{\link[=get_causal_order_stability]{get_causal_order_stability()}} (see its
documentation for details). This function does not expose a \code{regressor}
or \code{bw_method} argument, unlike the upstream Python implementation.
}
\examples{
confounded <- generate_parce_sample(n = 500, seed = 1)

result <- lingam_parce(confounded$data, reg_method = "ols")
print(result)

# The variable pair sharing the latent confounder is left unresolved (NA)
result$adjacency_matrix[confounded$confounded_pair, confounded$confounded_pair]

# Total effect estimation warns and returns NA for confounded variables
estimate_total_effect_parce(confounded$data, result,
  from_index = confounded$confounded_pair[1], to_index = 1
)
}
\references{
Tashiro, T., Shimizu, S., Hyvarinen, A., and Washio, T. (2014).
ParceLiNGAM: a causal ordering method robust against latent confounders.
Neural Computation, 26(1), 57-83.
}
