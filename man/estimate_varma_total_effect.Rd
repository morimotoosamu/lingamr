% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma_total_effect.r
\name{estimate_varma_total_effect}
\alias{estimate_varma_total_effect}
\title{Estimate a total causal effect in a VARMA-LiNGAM model}
\usage{
estimate_varma_total_effect(X, result, from_index, to_index, from_lag = 0)
}
\arguments{
\item{X}{original data (matrix or data frame), rows ordered in time; must
be the data the model was fitted on (the residuals stored in \code{result}
are aligned to it)}

\item{result}{a \code{VARMALiNGAMResult} from \code{\link[=lingam_varma]{lingam_varma()}}}

\item{from_index}{source variable (1-based index or variable name)}

\item{to_index}{destination variable (1-based index or variable name)}

\item{from_lag}{lag of the source variable (0 = current time, default)}
}
\value{
the estimated total effect (scalar)
}
\description{
Estimates the total causal effect from \code{from_index} (optionally at lag
\code{from_lag}) to \code{to_index} (at the current time) using the fitted
VARMA-LiNGAM model. Port of the Python reference \code{estimate_total_effect}:
the destination variable is regressed on the source variable together with
the source's parents (a back-door adjustment) over lagged X and lagged
residual regressors, and the source's coefficient is returned.
}
\details{
The Python reference requires the residual matrix \code{E} as an argument; here
it is reconstructed internally from \code{result$residuals} and the fitted
instantaneous matrix B0, so only the original data are needed.
}
\examples{
sample <- generate_varmalingam_sample(n = 1000, seed = 42)
model <- lingam_varma(sample$data,
  order = c(1, 1), criterion = NULL,
  reg_method = "ols", prune = FALSE
)

# total effect of x0 (current) on x2 (current)
estimate_varma_total_effect(sample$data, model, from_index = 1, to_index = 3)
}
