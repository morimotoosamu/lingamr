% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_regression.r
\name{lambda_scale_factor}
\alias{lambda_scale_factor}
\title{Scale factor used to make the (otherwise fixed, absolute) lambda search
grids adapt to the response's natural scale.}
\usage{
lambda_scale_factor(y)
}
\arguments{
\item{y}{response variable (numeric vector)}
}
\value{
a positive scale factor
}
\description{
\code{lasso_lambda_seq} / \code{ridge_lambda_seq} are fixed absolute grids. Because
glmnet's \code{standardize = TRUE} only standardizes the predictors (not the
response \code{y}), the penalty strength needed for meaningful shrinkage scales
with the magnitude of \code{y}. Without this scaling, multiplying the whole
input data by a constant changes which edges the default
\code{adaptive_lasso} + \code{lambda = "BIC"}/\code{"AIC"} pipeline selects, even though
the underlying relationships are identical up to that constant.
}
\keyword{internal}
