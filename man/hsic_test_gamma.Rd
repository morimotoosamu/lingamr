% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsic.r
\name{hsic_test_gamma}
\alias{hsic_test_gamma}
\title{HSIC independence test with gamma approximation}
\usage{
hsic_test_gamma(X, Y)
}
\arguments{
\item{X}{numeric vector}

\item{Y}{numeric vector (same length as X)}
}
\value{
list(stat = HSIC test statistic, p = gamma-approximated p-value)
}
\description{
Faithful port of \code{hsic_test_gamma()} (hsic.py). O(n^2) in the sample size
because it forms the full n x n Gram matrices; not recommended for n in
the thousands.
}
\details{
The gamma-approximation variance estimator is only defined for n >= 6
(its closed form divides by \code{(n-1)(n-2)(n-3)}); below that this errors
instead of silently returning a NaN p-value that would otherwise
propagate into \code{NA}-valued rejection decisions in callers. A constant
input (zero variance) carries no dependence information, so it is
treated as trivially independent (\code{p = 1}) rather than routed through
the degenerate kernel-width / centered-Gram-matrix computation.
}
\keyword{internal}
