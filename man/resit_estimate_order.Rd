% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_resit.r
\name{resit_estimate_order}
\alias{resit_estimate_order}
\title{RESIT step 1: estimate the topological (causal) order}
\usage{
resit_estimate_order(X, reg_fn, partial_orders, Aknw)
}
\arguments{
\item{X}{data matrix}

\item{reg_fn}{regressor function from \code{\link[=resit_make_regressor]{resit_make_regressor()}}}

\item{partial_orders}{(from, to) matrix from \code{\link[=extract_partial_orders]{extract_partial_orders()}},
or NULL}

\item{Aknw}{validated prior-knowledge matrix (negative entries already
NA), or NULL}
}
\value{
list(pa = list of parent index vectors per variable,
order = causal order, source first)
}
\description{
Faithful port of \code{RESIT._estimate_order()} (resit.py). Repeatedly finds
the most sink-like variable: the one whose regression residual on all
other remaining variables has the smallest HSIC dependence statistic.
Sinks are identified first and prepended, so the returned order runs
from source to sink.
}
\details{
Deviation from the Python original: \code{\link[=parce_search_candidate]{parce_search_candidate()}} falls
back to the full remaining set when prior knowledge excludes every
candidate (the Python code would crash on an empty argmin).
}
\keyword{internal}
