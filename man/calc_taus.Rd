% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_high_dim.r
\name{calc_taus}
\alias{calc_taus}
\title{Conditional non-Gaussianity statistics (tau), minimized over all
conditioning subsets}
\usage{
calc_taus(Y, yty, pa, ch, k, cond_sets, an_sets)
}
\arguments{
\item{Y}{data matrix}

\item{yty}{cached Gram matrix \code{t(Y) \%*\% Y}}

\item{pa}{index of the candidate parent variable (scalar)}

\item{ch}{indices of candidate child variables}

\item{k}{moment degree}

\item{cond_sets}{list of conditioning sets (each an integer vector of
1-based column indices, always including \code{last_root})}

\item{an_sets}{list, parallel to \code{cond_sets}, of ancestor-candidate sets
(conditioning-set variables excluded from the corresponding subset)}
}
\value{
numeric vector of length \code{ncol(Y)}, minimum statistic per variable
}
\description{
Per Wang & Drton (2020), the pruning statistic for a candidate
variable is the minimum of the conditional tau statistic over every
size-appropriate conditioning subset. Upstream HighDimDirectLiNGAM
(cdt15/lingam) has a \code{return} mis-indented inside its loop over
conditioning sets, so it only ever evaluates the first subset; this R
port intentionally does NOT replicate that bug and instead evaluates
every subset in \code{cond_sets}, so results differ numerically from the
Python package (see \code{dev/high-dim-direct-lingam-implementation.md}).
}
\keyword{internal}
