% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_lim.r
\name{lim_likelihood_i}
\alias{lim_likelihood_i}
\title{Local log-likelihood of continuous component i (super-Gaussian disturbance)}
\usage{
lim_likelihood_i(X, i, b_full, b0)
}
\description{
Direct port of \code{likelihood_i}/\code{log_p_super_gaussian}/\code{variance_i} from
\verb{lingam/utils/__init__.py} (lines 866-939 of the Python source).
}
\details{
A (near-)zero residual variance means component i is fit deterministically
by its parents; \code{-n * log(sqrt(var_i))} would then diverge to \code{Inf} and
make this candidate model look artificially superior. Such candidates are
instead scored with a large finite penalty so they lose every BIC
comparison in \code{\link[=lim_bic_loss]{lim_bic_loss()}}/\code{\link[=lim_local_search]{lim_local_search()}} without introducing
\code{Inf}/\code{NaN} into the running score.
}
\keyword{internal}
