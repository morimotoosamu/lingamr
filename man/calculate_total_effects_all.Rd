% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.r
\name{calculate_total_effects_all}
\alias{calculate_total_effects_all}
\title{Compute all pairwise total causal effects at once}
\usage{
calculate_total_effects_all(adjacency_matrix)
}
\arguments{
\item{adjacency_matrix}{Adjacency matrix (p x p). \code{B[i,j]} is the
coefficient of j -> i; NAs are treated as absent edges.}
}
\value{
p x p matrix \code{TE} with \code{TE[to, from]} = total effect of from on to
}
\description{
Equivalent to calling \code{\link[=calculate_total_effect]{calculate_total_effect()}} for every (from, to)
pair, but computed as the truncated Neumann series \code{B + B^2 + ... + B^(p-1)}. In a DAG every walk is a simple path (a repeated node would
imply a cycle), so the series contains exactly the same path-product
terms the DFS enumerates, and entries with no connecting path are exactly
zero. Only valid for acyclic \code{B} -- with cycles the series would count
non-simple walks that \code{\link[=find_all_paths]{find_all_paths()}} excludes.
}
\keyword{internal}
