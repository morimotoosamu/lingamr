% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma_bootstrap.r
\name{lingam_varma_bootstrap}
\alias{lingam_varma_bootstrap}
\title{Bootstrap for VARMA-LiNGAM}
\usage{
lingam_varma_bootstrap(
  X,
  n_sampling,
  order = c(1L, 1L),
  criterion = "bic",
  measure = "pwling",
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  prune = TRUE,
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{X}{numeric matrix or data frame (n_samples x n_features), rows ordered
in time.}

\item{n_sampling}{number of bootstrap iterations (positive integer).}

\item{order}{VARMA order \code{c(p, q)}. When \code{criterion} is not NULL, the order
is selected once on the original data and then fixed across all iterations.}

\item{criterion}{order-selection criterion ("bic", "aic", "hqic") or NULL to
use \code{order} directly.}

\item{measure}{independence measure for \code{\link[=lingam_direct]{lingam_direct()}} ("pwling"/"kernel").}

\item{reg_method}{regression method for the instantaneous matrix.}

\item{lambda}{penalty selection (see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{init_method}{initial-weight method for adaptive LASSO.}

\item{prune}{logical; passed to \code{\link[=lingam_varma]{lingam_varma()}} on each iteration (default TRUE).}

\item{seed}{random seed (NULL allowed).}

\item{verbose}{whether to print progress (logical).}

\item{parallel}{whether to distribute iterations across cores (logical).}

\item{n_cores}{number of cores (integer or NULL; NULL caps at 2 for safety).}
}
\value{
a \code{VARMABootstrapResult} object.
}
\description{
Evaluates the statistical reliability of the estimated time-series DAG by
resampling. Like \code{\link[=lingam_var_bootstrap]{lingam_var_bootstrap()}}, this uses a \strong{residual
bootstrap}: the VARMA model is fitted once on the original data, the
residuals are resampled with replacement, and a new series is rebuilt by
the VARMA recursion before re-estimating VARMA-LiNGAM on it. Port of the
Python reference \code{VARMALiNGAM.bootstrap}.
}
\details{
Reproducibility follows the same rules as \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}: with
\code{parallel = TRUE}, L'Ecuyer streams via \code{parallel::clusterSetRNGStream()} make
results reproducible for a given \code{seed} and \code{n_cores}, but they do not match
the sequential (\code{parallel = FALSE}) results.

\strong{On iteration failures:} as in \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}, each iteration
runs inside a \code{tryCatch()}; a failing iteration is reported as a warning and
excluded from the result instead of aborting the run. An error is raised
only if every iteration fails.

As in the Python reference, the series regeneration omits the estimated
intercept, so resampled series are centered near zero even when the
original data are not; each refit re-estimates its own intercept, so the
resampled coefficient estimates are unaffected.

Total effects are estimated by the back-door regression of
\code{\link[=estimate_varma_total_effect]{estimate_varma_total_effect()}} (the Python reference does the same) and
cover the instantaneous block and the AR lags 1..p; the MA (omega) blocks
describe effects of past disturbances, not of observed variables, and are
therefore excluded from \code{total_effects}.
}
\examples{
s <- generate_varmalingam_sample(n = 300, seed = 42)

# Fast example: OLS instantaneous structure, no pruning (no glmnet needed)
bs <- lingam_varma_bootstrap(s$data,
  n_sampling = 5L, order = c(1, 1), criterion = NULL,
  reg_method = "ols", prune = FALSE, seed = 1, verbose = FALSE
)
get_varma_probabilities(bs)
}
