% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_parce_sample.r
\name{generate_parce_sample}
\alias{generate_parce_sample}
\title{Generate sample data with a latent confounder (for BottomUpParceLiNGAM)}
\usage{
generate_parce_sample(n = 1000L, seed = NULL)
}
\arguments{
\item{n}{number of samples (default: 1000)}

\item{seed}{random seed (default: NULL, i.e. do not reset the RNG state)}
}
\value{
list with three elements:
\itemize{
\item \code{data}: data.frame of the 6 observed variables (\code{x0}-\code{x5}).
\item \code{adjacency_matrix}: the true 6x6 adjacency matrix among the observed
variables, following the \code{m[to, from]} convention. The \code{x2}-\code{x3} entries
(which share the latent confounder \code{x6} and have no direct edge between
them) are \code{NA}, matching the convention used by \code{\link[=lingam_parce]{lingam_parce()}}.
\item \code{confounded_pair}: 1-based column positions of \code{x2} and \code{x3} (the pair
sharing the latent confounder).
}
}
\description{
Generates the 7-variable model used in the ParceLiNGAM tutorial, where
\code{x6} is an unobserved (latent) common cause of \code{x2} and \code{x3}. Only
\code{x0}-\code{x5} are returned as observed data; \code{x6} is not included.
}
\details{
The data-generating process (all error terms are \code{Uniform(0, 1)}):
\preformatted{
x6 (latent) ~ Uniform(0, 1)
x3 = 2.0 * x6 + e
x2 = 2.0 * x6 + e
x0 = 0.5 * x3 + e
x1 = 0.5 * x0 + 0.5 * x2 + e
x5 = 0.5 * x0 + e
x4 = 0.5 * x0 - 0.5 * x2 + e
}
}
\examples{
confounded <- generate_parce_sample(n = 500, seed = 42)
head(confounded$data)
confounded$adjacency_matrix
confounded$confounded_pair
}
