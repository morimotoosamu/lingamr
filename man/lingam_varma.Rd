% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma.r
\name{lingam_varma}
\alias{lingam_varma}
\title{VARMA-LiNGAM for time series causal discovery}
\usage{
lingam_varma(
  X,
  order = c(1L, 1L),
  criterion = "bic",
  measure = "pwling",
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  prune = TRUE,
  ar_coefs = NULL,
  ma_coefs = NULL
)
}
\arguments{
\item{X}{numeric matrix or data frame (n_samples x n_features). Rows are
ordered in time (earliest first).}

\item{order}{VARMA order \code{c(p, q)} (AR and MA lags, non-negative integers,
not both zero). When \code{criterion} is not NULL, the best order in
\verb{0:p x 0:q} (excluding \code{c(0, 0)}) is selected by the information
criterion; otherwise \code{order} is used directly.}

\item{criterion}{order-selection criterion ("bic", "aic", or "hqic"), or
NULL to use \code{order} directly without selection.}

\item{measure}{independence measure passed to \code{\link[=lingam_direct]{lingam_direct()}}
("pwling" or "kernel").}

\item{reg_method}{regression method for the instantaneous adjacency matrix:
"adaptive_lasso" (default), "lasso", "ols", or "ridge" (see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{lambda}{penalty (lambda) selection for the instantaneous matrix:
"BIC" (default), "AIC", "lambda.min", "lambda.1se", or "oracle"
(see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{init_method}{initial-weight method for adaptive LASSO
(see \code{\link[=lingam_direct]{lingam_direct()}}).}

\item{prune}{logical; if \code{TRUE} (default, matching the Python reference),
all adjacency matrices (psi and omega) are refined together by adaptive
LASSO so weak edges are shrunk toward zero. Requires the glmnet package.
Set \code{FALSE} to keep the raw transformed matrices (no glmnet needed when
\code{reg_method = "ols"}).}

\item{ar_coefs}{optional known AR coefficient array (p, n_features,
n_features) in reduced form. Must be supplied together with \code{ma_coefs};
when both are given the VARMA estimation step is skipped, \code{order} is
derived from the arrays, and \code{criterion} is ignored.}

\item{ma_coefs}{optional known MA coefficient array (q, n_features,
n_features) in reduced form. See \code{ar_coefs}.}
}
\value{
A \code{VARMALiNGAMResult} object (list) containing:
\itemize{
\item \code{adjacency_matrices}: list with two arrays.
\code{psis} (1 + p, n_features, n_features): slice \verb{[1, , ]} is the
instantaneous matrix B0 (= psi_0) and slice \verb{[tau + 1, , ]} is the lagged
matrix psi_tau. \code{omegas} (q, n_features, n_features): slice \verb{[w, , ]} is
the MA causal matrix omega_w. Convention: entry \verb{[i, j]} is the effect
from variable j to variable i.
\item \code{causal_order}: estimated causal order of the instantaneous structure
(1-based indices).
\item \code{residuals}: filtered VARMA residuals n_t
(n_samples - max(p, q), n_features).
\item \code{order}: the VARMA order \code{c(p, q)} actually used.
\item \code{ar_coefs}, \code{ma_coefs}: reduced-form coefficient arrays Phi (p, m, m) and
Theta (q, m, m) used to derive the causal matrices.
\item \code{const}: estimated intercept vector of the reduced-form VARMA (zero when
\code{ar_coefs}/\code{ma_coefs} are supplied).
}
}
\description{
Fits a vector autoregressive moving-average (VARMA) model to time series
data and applies Direct LiNGAM to the residuals to recover the
instantaneous (lag-0) causal structure. The lagged causal matrices (psi)
and the moving-average causal matrices (omega) are then derived from the
VARMA coefficients and the instantaneous structure.
}
\details{
The structural model is
\verb{x_t = B0 x_t + sum_\{tau=1\}^\{p\} psi_tau x_\{t-tau\} + e_t + sum_\{w=1\}^\{q\} omega_w e_\{t-w\}}, where B0 is the instantaneous effect matrix
(strictly acyclic) and e_t are mutually independent non-Gaussian errors.
The reduced-form VARMA
\verb{x_t = c + sum Phi_tau x_\{t-tau\} + n_t + sum Theta_w n_\{t-w\}} is estimated
first; Direct LiNGAM applied to the residuals n_t yields B0, and the causal
matrices follow \verb{psi_tau = (I - B0) Phi_tau} and
\verb{omega_w = (I - B0) Theta_w (I - B0)^\{-1\}}.

Differences from the Python reference implementation:
\itemize{
\item VARMA coefficients are estimated by the two-stage Hannan-Rissanen
procedure (a long VAR yields residual estimates, then X is regressed on
its own lags and the lagged residuals, both stages with an intercept)
instead of state-space maximum likelihood (statsmodels \code{VARMAX}).
Estimates are consistent but not identical to the Python output; the
\code{max_iter} parameter of the Python class has no counterpart here because
the procedure is non-iterative.
\item The MA causal matrices use the full similarity transform
\verb{omega_w = (I - B0) Theta_w (I - B0)^\{-1\}}. The Python implementation
drops the trailing inverse factor due to a three-argument \code{np.dot} call
(the third argument is treated as an output buffer), so its unpruned
omegas equal \verb{(I - B0) Theta_w}.
\item Residual filtering initializes the first \code{max(p, q)} residuals with
zeros instead of random draws, making the fit deterministic.
}
}
\examples{
sample <- generate_varmalingam_sample(n = 300, seed = 1)

# OLS instantaneous structure without pruning (no extra packages required)
model <- lingam_varma(sample$data,
  order = c(1, 1), criterion = NULL,
  reg_method = "ols", prune = FALSE
)
model$causal_order
round(model$adjacency_matrices$psis[1, , ], 2) # instantaneous B0
}
\references{
Kawahara, Y., Shimizu, S., & Washio, T. (2011). Analyzing relationships
among ARMA processes based on non-Gaussianity of external influences.
\emph{Neurocomputing}, 74(12-13), 2212-2221. Ported from the Python
implementation cdt15/lingam (\url{https://github.com/cdt15/lingam}).
}
