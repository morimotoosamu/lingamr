% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.r
\name{tidy.MultiGroupLingamResult}
\alias{tidy.MultiGroupLingamResult}
\title{Convert a MultiGroupLingamResult to a tidy data.frame}
\usage{
\method{tidy}{MultiGroupLingamResult}(x, threshold = 0, ...)
}
\arguments{
\item{x}{The return value of \code{\link[=lingam_multi_group]{lingam_multi_group()}}
(a \code{MultiGroupLingamResult} object)}

\item{threshold}{Coefficients with an absolute value at or below this are not
treated as edges (default: 0)}

\item{...}{Unused}
}
\value{
data.frame(group, from, to, estimate)
}
\description{
Stacks the per-group edge lists into a single long-format data.frame with a
\code{group} column in front, one edge per row per group (the causal order is
shared across groups but the coefficients differ).
}
\examples{
mg <- generate_multi_group_sample()
model <- lingam_multi_group(mg$data_list, reg_method = "ols")
tidy(model)
}
