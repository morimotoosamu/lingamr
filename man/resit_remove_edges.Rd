% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_resit.r
\name{resit_remove_edges}
\alias{resit_remove_edges}
\title{RESIT step 2: remove superfluous edges}
\usage{
resit_remove_edges(X, pa, pi_order, reg_fn, alpha)
}
\arguments{
\item{X}{data matrix}

\item{pa}{list of parent index vectors from \code{\link[=resit_estimate_order]{resit_estimate_order()}}}

\item{pi_order}{causal order (source first)}

\item{reg_fn}{regressor function}

\item{alpha}{significance level of the HSIC test}
}
\value{
pruned \code{pa} list
}
\description{
Faithful port of \code{RESIT._remove_edges()} (resit.py). For each variable in
causal order (skipping the source) and each of its candidate parents \code{l},
the variable is regressed on its current parents excluding \code{l}; if the
residual is independent (HSIC p-value > alpha) of the variable's original
parent set, the edge from \code{l} is dropped.
}
\details{
Note the two distinct parent sets, mirroring the original exactly:
\code{parents_snapshot} is the parent set frozen before the inner loop and is
always the second HSIC argument, while \code{predictors} is derived from the
\emph{current} (already pruned) parent set minus \code{l}.
}
\keyword{internal}
