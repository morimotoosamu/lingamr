% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var_bootstrap.r
\name{get_var_paths}
\alias{get_var_paths}
\title{Enumerate bootstrap paths between two variables in a VAR-LiNGAM model}
\usage{
get_var_paths(
  result,
  from_index,
  to_index,
  from_lag = 0,
  to_lag = 0,
  min_causal_effect = NULL
)
}
\arguments{
\item{result}{a VARBootstrapResult object}

\item{from_index}{source variable (1-based)}

\item{to_index}{destination variable (1-based)}

\item{from_lag}{lag of the source (default 0)}

\item{to_lag}{lag of the destination (default 0); must satisfy \code{to_lag <= from_lag}}

\item{min_causal_effect}{minimum |effect| threshold (NULL = 0)}
}
\value{
a data frame (path, effect, probability), one row per distinct path
}
\description{
Builds the time-expanded graph for every bootstrap sample and enumerates all
directed paths from the source (at \code{from_lag}) to the destination (at
\code{to_lag}), reporting each path's bootstrap probability and median effect.
Port of the Python reference \code{VARBootstrapResult.get_paths}.
}
\details{
Node indices in the returned \code{path} are 1-based positions in the time-expanded
graph: column j of block L (lag L) corresponds to index \code{n_features * L + j}.
}
\examples{
s <- generate_varlingam_sample(n = 500, seed = 42)
bs <- lingam_var_bootstrap(s$data,
  n_sampling = 10L, criterion = NULL,
  reg_method = "ols", prune = FALSE, seed = 1, verbose = FALSE
)
get_var_paths(bs, from_index = 1, to_index = 3)
}
