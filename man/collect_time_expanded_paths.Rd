% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_var_bootstrap.r
\name{collect_time_expanded_paths}
\alias{collect_time_expanded_paths}
\title{Enumerate and aggregate paths over time-expanded bootstrap graphs}
\usage{
collect_time_expanded_paths(
  ams,
  nf,
  n_lags,
  from_index,
  to_index,
  from_lag,
  to_lag,
  min_causal_effect
)
}
\arguments{
\item{ams}{list of joined lag matrices (n_features x n_features*(1 + n_lags));
block k + 1 holds the lag-k coefficients}

\item{nf}{number of features}

\item{n_lags}{number of lag blocks beyond the instantaneous one}

\item{from_index}{source variable (1-based)}

\item{to_index}{destination variable (1-based)}

\item{from_lag}{lag of the source}

\item{to_lag}{lag of the destination}

\item{min_causal_effect}{minimum |effect| threshold}
}
\value{
a data frame (path, effect, probability), one row per distinct path
}
\description{
Shared core of \code{\link[=get_var_paths]{get_var_paths()}} and \code{\link[=get_varma_paths]{get_varma_paths()}}: builds the
time-expanded square graph for each bootstrap adjacency matrix, enumerates
all directed paths from the source (at \code{from_lag}) to the destination (at
\code{to_lag}), and aggregates each distinct path's bootstrap probability and
median effect.
}
\keyword{internal}
