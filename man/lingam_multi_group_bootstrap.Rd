% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_multi_group_bootstrap.r
\name{lingam_multi_group_bootstrap}
\alias{lingam_multi_group_bootstrap}
\title{Bootstrap for Multi-Group Direct LiNGAM}
\usage{
lingam_multi_group_bootstrap(
  X_list,
  n_sampling,
  prior_knowledge = NULL,
  apply_prior_knowledge_softly = FALSE,
  reg_method = "adaptive_lasso",
  lambda = "BIC",
  init_method = "ols",
  seed = NULL,
  verbose = TRUE,
  parallel = FALSE,
  n_cores = NULL,
  compute_total_effects = TRUE
)
}
\arguments{
\item{X_list}{A list of numeric matrices or data frames (length >= 2), one
per group. Same requirements as \code{\link[=lingam_multi_group]{lingam_multi_group()}}.}

\item{n_sampling}{Number of bootstrap iterations}

\item{prior_knowledge}{Prior knowledge matrix (NULL allowed). Applied to
every group, same as \code{\link[=lingam_multi_group]{lingam_multi_group()}}.}

\item{apply_prior_knowledge_softly}{Apply prior knowledge softly (logical)}

\item{reg_method}{Regression method ("ols", "lasso", "adaptive_lasso", "ridge")}

\item{lambda}{Lambda selection ("lambda.min", "lambda.1se", "AIC", "BIC", "oracle")}

\item{init_method}{Method for estimating the initial weights of adaptive LASSO
regression ("ols" or "ridge")}

\item{seed}{Random seed (NULL allowed)}

\item{verbose}{Whether to display progress (logical)}

\item{parallel}{Whether to use parallel processing (logical)}

\item{n_cores}{Number of cores to use (integer, NULL allowed). When \code{NULL},
the number of cores is limited to a maximum of 2 for safety. Ignored when
\code{parallel = FALSE}.}

\item{compute_total_effects}{Whether to also compute total causal effects
for every variable pair on each bootstrap iteration (logical, default
\code{TRUE}). Set to \code{FALSE} to skip it when only edge/order stability is
needed.}
}
\value{
A \code{MultiGroupBootstrapResult}: a named list (one element per group)
of \code{BootstrapResult} objects (see \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}), with
class \code{"MultiGroupBootstrapResult"}.
}
\description{
Bootstrap for Multi-Group Direct LiNGAM
}
\details{
Each element of the returned list is a regular \code{BootstrapResult}, so the
existing single-group bootstrap functions (\code{\link[=get_probabilities]{get_probabilities()}},
\code{\link[=get_causal_direction_counts]{get_causal_direction_counts()}}, \code{\link[=get_directed_acyclic_graph_counts]{get_directed_acyclic_graph_counts()}},
\code{\link[=get_total_causal_effects]{get_total_causal_effects()}}, \code{\link[=get_causal_order_stability]{get_causal_order_stability()}},
\code{\link[=plot_bootstrap_probabilities]{plot_bootstrap_probabilities()}}, \code{tidy()}) all work by extracting a group
with \code{result[[group_name]]} or \code{result[[i]]}, mirroring the upstream Python
API (which returns a list of \code{BootstrapResult} per group).

\strong{Total effects use path products, not regression.} Each bootstrap
iteration's total-effect matrix is the sum of path-coefficient products
over the DAG defined by that iteration's adjacency matrix (matching the
upstream Python MultiGroup bootstrap), which is a different method from
\code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}'s regression-based
\code{\link[=estimate_all_total_effects]{estimate_all_total_effects()}}.

\strong{On iteration failures:} as in \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}, each
iteration is wrapped in \code{tryCatch()}; a failing iteration is skipped with a
warning, and only if every iteration fails is an error raised.

\strong{On reproducibility:} same policy as \code{\link[=lingam_direct_bootstrap]{lingam_direct_bootstrap()}}. During
parallel execution, L'Ecuyer parallel random number streams via
\code{parallel::clusterSetRNGStream()} are used. Results are reproducible given
the same \code{seed} and same \code{n_cores}, but they do not numerically match the
results of sequential execution (\code{parallel = FALSE}). If you need results
that exactly match the sequential version, use \code{parallel = FALSE}.
}
\examples{
mg <- generate_multi_group_sample()

bs <- lingam_multi_group_bootstrap(mg$data_list,
  n_sampling = 10L,
  reg_method = "ols",
  seed = 42
)
get_probabilities(bs[[1]])

\donttest{
bs_par <- lingam_multi_group_bootstrap(mg$data_list,
  n_sampling = 30L,
  reg_method = "ols",
  seed = 42,
  parallel = TRUE,
  n_cores = 2L
)
}
}
