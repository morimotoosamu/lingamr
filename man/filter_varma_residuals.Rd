% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_varma.r
\name{filter_varma_residuals}
\alias{filter_varma_residuals}
\title{Filter VARMA residuals by deterministic recursion}
\usage{
filter_varma_residuals(X, phis, thetas, const = NULL)
}
\arguments{
\item{X}{numeric matrix (n_samples x n_features), rows ordered in time}

\item{phis}{AR coefficient array (p, m, m)}

\item{thetas}{MA coefficient array (q, m, m)}

\item{const}{intercept vector (NULL = zero)}
}
\value{
full-length residual matrix (n_samples x n_features); the first
\code{max(p, q)} rows are zero
}
\description{
Computes \verb{n_t = x_t - c - sum Phi_tau x_\{t-tau\} - sum Theta_w n_\{t-w\}}
recursively. The first \code{max(p, q)} residuals are initialized with zeros
(the Python reference draws them from a standard normal, which makes the
fit non-deterministic; the transient effect dies out at the same rate
either way).
}
\keyword{internal}
