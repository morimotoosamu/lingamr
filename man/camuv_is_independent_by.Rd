% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lingam_camuv.r
\name{camuv_is_independent_by}
\alias{camuv_is_independent_by}
\title{Independence judgment with an explicit threshold, returning the value}
\usage{
camuv_is_independent_by(X, Y, threshold, independence)
}
\arguments{
\item{X}{numeric vector or single/multi-column matrix}

\item{Y}{numeric vector or single/multi-column matrix}

\item{threshold}{rejection threshold (see above)}

\item{independence}{"hsic" or "fcorr"}
}
\value{
list(independent = logical, value = numeric)
}
\description{
Faithful port of \code{CAMUV._is_independent_by()}. For \code{"hsic"} the value is
the gamma-approximated p-value and independence means \code{value > threshold};
for \code{"fcorr"} the value is the F-correlation and independence means
\code{value < threshold}. Note the opposite directions.
}
\details{
\code{f_correlation()} is univariate, so the fcorr path requires single-column
inputs; \code{\link[=lingam_camuv]{lingam_camuv()}} rejects \code{independence = "fcorr"} with
\code{num_explanatory_vals > 2} up front, which is the only way a multivariate
input could reach this point (the upstream implementation breaks on that
combination too).
}
\keyword{internal}
