# Validate the return value of an `imputer` function

Validate the return value of an `imputer` function

## Usage

``` r
validate_imputer_output(datasets, X_boot)
```

## Arguments

- datasets:

  Return value of `imputer(X_boot)`

- X_boot:

  The bootstrap-resampled data passed to `imputer`
