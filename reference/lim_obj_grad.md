# Value and gradient of the augmented-Lagrangian objective for the doubled parameter vector w (length 2*d*d)

Value and gradient of the augmented-Lagrangian objective for the doubled
parameter vector w (length 2*d*d)

## Usage

``` r
lim_obj_grad(w, d, X, con, W_dis_mask, W_con_mask, rho, alpha, lambda1)
```
