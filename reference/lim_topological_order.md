# Topological order (Kahn's algorithm) from a lingamr-convention adjacency matrix B (`B[i, j]` = j -\> i, i.e. row i's nonzero columns are its parents)

Topological order (Kahn's algorithm) from a lingamr-convention adjacency
matrix B (`B[i, j]` = j -\> i, i.e. row i's nonzero columns are its
parents)

## Usage

``` r
lim_topological_order(B)
```
